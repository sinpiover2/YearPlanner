# Amplify IM1 Import — Implementation Specification

**Document Status:** Implementation Specification — D-1 through D-5 approved; Implementation Sprints 1 and 2 complete (see "Sprint Progress," below)
**Governed by:** `docs/Architecture/CURRICULUM_INFORMATION_MODEL.md` (the architectural decisions this spec implements — read that first)
**Source data:** `Curriculm/M1/IM1_Curriculum_Extraction.md` (7 units, verified against Amplify's own PDFs)
**Scope:** How to evolve the current `Units`/`Lessons` schema and its consumers into the documented Instructional Item model, and how to import the completed IM1 extraction — without breaking current classroom use.

> Every "proposed," "recommended," or "should" statement not explicitly marked as implemented below is still a design decision, not a description of current behavior. Where this document describes what exists today, it cites the actual file and line so that claim can be checked directly.

---

## Sprint Progress

**Implementation Sprint 1 — complete.** Scope: backward-compatible schema contract and read compatibility only. No importer, no production data, no production writes, no user-visible behavior change. See `docs/Architecture/CURRICULUM_INFORMATION_MODEL.md`, "Sprint 1 Implementation Note," for the architecture-level summary; full detail is in the Sprint 1 handoff/report.

Implemented this sprint:
- `apps-script-planning/Code.js`: `addLesson` now writes `Type` (defaulting to `"Lesson"`) when a `Type` column exists on the sheet — a no-op today, since that column doesn't exist in production yet.
- `apps-script-planning/Code.js`: `deleteLesson` and `reorderLessons` no longer renumber `LessonNumber` from row position — only `SortOrder` is renumbered now. `LessonNumber` is preserved as stable publisher identity, per this spec's §2 and §9.
- `frontend/src/utils/plannerUtils.js`: added `parseKnownNumber`, `getItemType`, `getPlacementRule`, `hasFixedPlacement` — all new, all currently unused by any consumer, ready for Sprint 2.
- `frontend/src/utils/forecastModel.js`: added `TODO(Sprint 2)` comments at every silent-zero-coercion site (§10) — no logic changed.

Not yet done — remains Sprint 2+ per the Implementation Sequence (§10): Type/PlacementRule actually consumed by any UI or Forecast logic; flexible-placement exclusion wired in; incomplete-data Forecast state; the importer itself; anything touching production data.

**Implementation Sprint 2 — complete.** Scope: wire the Sprint 1 plumbing into real consumers, close the *silence* around zero-coercion (not the coercion itself — see below), add subtle Type display (D-3) — all behind conditions that don't fire for current classroom data. Still no importer, no production data, no production writes.

Implemented this sprint:
- `frontend/src/utils/plannerUtils.js`: added `isOptionalItem()` (reads `IsOptional`) and `getSequencedItems()` (filters to fixed-placement items via `hasFixedPlacement()`, then `sortLessons()`) — the one safe way to get a strict sequential walk. Resolved the two remaining Sprint 1 TODOs (`getRequiredDays`/`getOptionalDays` now use `parseKnownNumber()`; both functions are currently unused by any renderer, confirmed by inspection).
- `frontend/src/utils/forecastModel.js`: `getSectionForecast`/`getSectionTimeline` now call `getSequencedItems()` instead of `sortLessons()` directly, so flexible-placement items (Amplify's `Investigate`) never enter the sequential walk or get a fabricated position. Current-item selection now skips an unfinished item that `isOptionalItem()` reports as optional (D-1), continuing to look for the next required one.
- **Precisely what changed about the day-math, and what didn't:** every site that used to read `Number(x || 0)` directly now reads through `parseKnownNumber()` first, so an unknown value is recognized as unknown (`null`), not silently treated as identical to a real zero, *at the point of detection*. A `dataComplete` boolean is computed from that detection — `true` only when every relevant unit's day budget and every finished lesson's `PlannedDays` are confirmed — and surfaced in the UI as a short factual note when `false`. **The arithmetic itself was not redesigned**: once detected, an unknown value still falls back to `0` for the actual sum (`parseKnownNumber(x) ?? 0`), the same fallback as before, so the totals and variance Forecast computes internally are unchanged continuity math, not nullable-aware math propagated end-to-end. What changed is that this fallback is no longer *silent* — every consumer of that section's forecast can now check `dataComplete` and knows the number is a placeholder, where before there was no way to tell. Propagating `null` all the way through the arithmetic (rather than falling back to `0` for continuity) remains undone and is not assumed by anything built this sprint.
- `frontend/src/utils/forecastCardUtils.js` / `frontend/src/components/ForecastSummaryCards.jsx`: when `dataComplete` is `false`, the card shows a short factual note ("Planning days incomplete") — reusing the existing card typography, no new component. Not Forecast interface redesign — an additive line, conditional, invisible when data is complete (true of all current Math 8/IM1 data).
- `frontend/src/utils/planningModel.js`: unit-lesson sourcing now goes through `getSequencedItems()` (same flexible-item exclusion as Forecast, so a flexible item is excluded from the computed fixed sequence exactly as in Forecast); the Planning shelf's items now carry a normalized `type` field; `getCurriculumLessonLabel()` prefixes the Instructional Item Type when it isn't the default `Lesson` (D-3). **No alternate interface for flexible items exists yet** — a flexible item is excluded from the sequence, not given anywhere else to appear; this is a documented gap, not a silent omission (§8 already recorded this; restated here for the same reason). **The computed Planning "shelf" (`getPlanningModel()`'s `shelf` return value) is not presently rendered by any component in the application** — confirmed by inspection: no `.jsx` file reads `planningModel.shelf` today. This sprint's Type-awareness on the shelf is therefore currently inert data, not a visible feature; it is ready for whichever future sprint renders the shelf. **The Type display that is actually visible in the currently-rendered Planning experience is limited to the curriculum citation label** (`SessionTile.jsx`'s "Curriculum · …" line, built from `getCurriculumLessonLabel()`) — this is the only Planning surface where D-3 has any visible effect today.
- `frontend/src/components/LessonTable.jsx` / `frontend/src/App.css`: Units now shows a small, neutral `.lesson-pill.type` badge next to a lesson's title when its Type isn't the default `Lesson` — invisible for every current row (D-3).

Validated this sprint: a scratch, non-committed fixture script (19 checks — the full Sprint 2 test matrix) confirms every rule above against representative rows for all 9 Amplify item types plus legacy/unknown/incomplete cases, without touching production data. `npm run build` and `npm run lint` both pass with no new errors introduced. Live browser click-through could not be completed — see the accompanying report for why (an environment limitation, not a code defect).

Not yet done at the end of Sprint 2 — became Sprint 3's scope: the staged intermediate artifact (D-4) and a read-only preview pipeline. Still outstanding after Sprint 3 (below): the actual write-capable importer; teacher day-entry UI (D-2); `PlannedDays` initialization (D-5); propagating nullable planning values through Forecast's arithmetic (rather than falling back to `0` for continuity); anything touching production data.

**Implementation Sprint 3 — Staged Importer (read-only) — complete.** Scope: produce the staged intermediate artifact called for by D-4, and a read-only preview pipeline that proves the artifact can be validated and transformed into an import plan accurately — before any spreadsheet write capability exists. No execute mode, no confirmation token, no backup/rollback execution, no Apps Script changes, no production reads or writes.

Implemented this sprint (all under `scripts/import-staging/`, artifact at `data/import-staging/amplify-im1.json` — see `scripts/import-staging/README.md` for the full file map and commands):
- `amplify-im1-source.mjs`: a hand-transcribed, one-time copy of `IM1_Curriculum_Extraction.md`'s Instructional Items tables into machine-readable form, per this document's own §7 recommendation against an automated Markdown parser. All 7 units, 162 fixed-placement items, and both flexible-placement `Investigate` items are represented; literal `Type` strings are preserved exactly as extracted (no normalization, no conceptual-role mapping in the data).
- `generate-artifact.mjs`: builds the JSON artifact deterministically from that transcription — no network access, no PDF re-parsing. Re-running it against an unchanged extraction document produces a byte-identical file (`--check` flag verifies this; no wall-clock timestamp is embedded in the artifact's content, only a SHA-256 of the extraction document's current bytes).
- **ID scheme**: `AMP-IM1-U{n}` for units, `AMP-IM1-U{n}-I{order}` for fixed items, `AMP-IM1-U{n}-F{index}` for flexible items — deliberately distinct from the existing placeholder seed `UnitID`s (`IM1-U1`…`IM1-U8`), since Extraction Note #1 already found those don't correspond 1:1 to Amplify's real units. Reconciling staging IDs with eventual production IDs remains an importer-phase decision.
- `validate-artifact.mjs`: structural checks (every required field present), global itemId/unitId collision detection, literal-Type checking (an unrecognized Type is a warning, never an error — matches §3's "must never crash, drop the row, or silently discard it"), ordering/placement validation (a fixed item must have an order XOR a flexible item a placementRule, never both, never neither; duplicate order values within a unit are rejected), and completeness validation against an independently-transcribed expected-count table (not derived circularly from the same transcription it's checking).
- `build-import-plan.mjs`: a pure function of `(artifact, destination)` — given a destination snapshot in the *documented* shape `getSheetData()` returns (per `SHEET_STRUCTURE.md`; this sprint exercises it only against fixtures, never a live read of `getSheetData()` itself), classifies every unit and item as `create` / `source-update` / `no-op` / `blocked`. Enforces the field-ownership map from `CURRICULUM_INFORMATION_MODEL.md` §8/§9 directly: an update is only proposed when no teacher-owned field (`PlannedDays`, `TeacherNotes`, `PrimaryLink`) is populated on the existing row; otherwise it's `blocked` with `reasons: ["preserve-teacher-fields"]`. A same-ID, different-title match is flagged `title-mismatch-warning` rather than silently accepted. Duplicate destination IDs and cross-course ID collisions are hard failures (`plan.blocked = true`), not silently resolved. Unit-level `RequiredDays`/`OptionalDays` are never proposed for update once a Unit row exists, per D-2/D-5 — they're populated only on a brand-new Unit's `create` row, explicitly labeled informational/teacher-to-confirm.
- `preview.mjs`: a CLI that reads the artifact, runs the validator, builds a plan against a destination fixture, and prints a human-readable summary (counts, warnings, sample rows, an explicit "This preview performed zero writes" statement) plus an optional full JSON report. No browser UI, no new Apps Script endpoint.
- `fixtures/empty-destination.json` and `fixtures/representative-destination.json`: the two required preview scenarios. The representative fixture exercises every plan branch in one pass — an exact-match no-op, a title-mismatch source-update, a teacher-field-protected block, a duplicate-ID hard fail, and a cross-course-ID hard fail — plus an untouched legacy Math 8 row proving the plan never references data outside the artifact's scope.
- `test.mjs`: 24 lightweight tests via Node's built-in `node:test`/`node:assert` (no new dependency) — determinism, all 7 units' expected item counts, Unit 1's confirmed-absent quiz, literal-Type preservation across both vocabularies, both `Investigate` items' flexible-placement shape, optional-Lesson preservation (Unit 6 Lesson 13, Unit 7 Lesson 1), day-budget status fidelity, every validator edge case (unknown Type, both/neither order-and-placementRule, duplicate IDs, duplicate order, malformed day-budget status), and every import-plan classification branch listed above. All 24 pass.

Validated this sprint: `node scripts/import-staging/generate-artifact.mjs --check` confirms determinism; `node --test scripts/import-staging/test.mjs` — 24/24 pass; both required preview runs (empty destination, representative destination) produced the expected classifications with zero unexplained discrepancies; `node --check` passes on every new file; no production Sheet was read or written at any point.

Not yet done at the end of Sprint 3 — became Sprint 4's scope: an execute-capable importer following `ProductionDataCleanup.js`'s guarded preview/execute/backup/lock pattern per §7. Still outstanding after Sprint 4 (below): every item under "Known limitations and unverified production assumptions."

**Implementation Sprint 4 — Guarded Execution Importer (structurally complete, not yet run against production) — complete.** Scope: build the write-capable Apps Script importer architecture — preview, guarded execute, locking, backup, post-write verification, explicit failure handling — and prove it structurally safe, deterministic, reviewable, and testable through local simulation only. **`executeAmplifyIm1Import()` has never been invoked against the real production spreadsheet.** Every guarantee below was proven against in-memory fakes (`scripts/import-staging/fake-spreadsheet.mjs`), never a live Google Sheet.

**Current status, stated precisely:** Ready for a controlled read-only production audit after checkpointing; not yet authorized for production execution.

**Generated Apps Script payload strategy.** Apps Script cannot read the repository filesystem at runtime, so `scripts/import-staging/generate-apps-script-payload.mjs` deterministically emits `apps-script-planning/AmplifyIm1ImportData.js` — a generated, do-not-hand-edit file exposing two frozen constants: `AMPLIFY_IM1_IMPORT_PAYLOAD` (the same JSON already in `data/import-staging/amplify-im1.json`, embedded verbatim so re-stringifying it reproduces the original file's exact bytes) and `AMPLIFY_IM1_IMPORT_METADATA` (`schemaVersion`, `artifactSha256` — a SHA-256 of the canonical JSON file's own bytes, distinct from the extraction document's hash already inside the artifact — `unitCount`, `itemCount`, and a derived `confirmationPhrase`). `--check` detects drift the same way `generate-artifact.mjs --check` does. Both generators share one code path (`generate-apps-script-payload.mjs` reads the already-generated canonical JSON rather than re-deriving from the source transcription), so there is exactly one place curriculum content is authored, never two.

**Importer module location.** `apps-script-planning/AmplifyIm1Importer.js` — a dedicated file, not folded into `Code.js`. Every function is prefixed `amplifyIm1` to avoid colliding with the shared global namespace every file in this Apps Script project occupies (Apps Script has no per-file scoping or import system). The file is split into two sections: pure logic (no `SpreadsheetApp`/`LockService`/`Utilities` calls — testable directly under Node) and an Apps Script I/O boundary (sheet reads, backups, the guarded write sequence). A `if (typeof module !== "undefined") { module.exports = {...} }` guard at the bottom is inert in the real Apps Script runtime (no `module` global there) and is what lets `scripts/import-staging/importer.test.mjs` `require()` this exact file — not a reimplementation of it — under Node.

**Public functions.** `previewAmplifyIm1Import()` (read-only), `executeAmplifyIm1Import(confirmation)` (guarded write; no default parameter, so an accidental zero-argument invocation — e.g. an Apps Script editor "Run" click — passes `confirmation` as `undefined`, which can never satisfy the exact-string-match confirmation check; this is deliberately simpler than `ProductionDataCleanup.js`'s placeholder-default-to-edit pattern, and at least as safe), `verifyAmplifyIm1Import()` (read-only, standalone).

**Confirmation contract.** `IMPORT_AMPLIFY_IM1_<first 12 hex chars of artifactSha256>_<unitCount>_<itemCount>`, computed once by the generator and stored in `AMPLIFY_IM1_IMPORT_METADATA.confirmationPhrase`. Exact `===` string match only (`amplifyIm1ValidateConfirmation_`) — no trim, no case-folding, no truthy/boolean acceptance. Because the phrase is derived from the artifact's own hash and counts, any change to the curriculum content invalidates every previously-copied confirmation string automatically; there is nothing to separately "expire." Shown to the operator by `previewAmplifyIm1Import()`'s report (`confirmationRequired`), never treated as a secret.

**Destination schema prerequisites.** `amplifyIm1ValidateDestinationSchema_` requires Units: `UnitID, CourseID, UnitName, RequiredDays, OptionalDays, SortOrder`; Lessons: `LessonID, UnitID, CourseID, LessonNumber, LessonTitle, PlannedDays, SortOrder, Type, PlacementRule, Description, PrimaryLink, TeacherNotes, IsOptional`. **`Type` and `PlacementRule` do not exist on the production Lessons sheet today** (§1's Sprint 1 note already recorded this: adding `Type` to `addLesson`'s write path was "a no-op today, since that column doesn't exist in production yet"). This means schema validation will legitimately report `blocked` against the real spreadsheet as it exists right now — that is correct, expected behavior, not a bug, and it is the single largest remaining prerequisite before any real preview or execution (see "Known limitations," below). No column is added silently by this importer at any point.

**Publisher-owned / teacher-owned field map** (identical to Sprint 3's, now enforced at write time, not just at plan-building time): publisher-owned — `UnitName`, item `Title`/`Type`/`Description`/`SortOrder`/`PlacementRule`/`IsOptional`. Teacher-owned, never written — Unit `RequiredDays`/`OptionalDays` (once the Unit row exists; populated only on a brand-new `create`, per D-2/D-5), Lesson `PlannedDays`, `TeacherNotes`, `PrimaryLink`. **`LessonNumber` is left blank on every created row** — the extraction does not carry a distinct `LessonNumber` field (only `Order`, plus an occasional "Lesson N:" fragment embedded in `Subtitle` text); deriving one by parsing that text would be inventing structured publisher data, which this project's extraction principles have forbidden from the start. This is a known, documented gap, not a silent guess — see "Known limitations."

**Current-state reading.** `amplifyIm1ReadSheet_` mirrors `apps-script-roster-admin/ProductionDataAudit.js`'s `auditReadSheet_` shape exactly (`present/headers/rawRows/objects/rowCount`) — that file lives in a separate Apps Script project and cannot be imported, so this is a deliberate, small, already-reviewed-pattern duplication, not a new invention. Reads happen once per pass (schema validation and the planning pass share one read; the revalidation pass re-reads fresh immediately before writing).

**Plan construction — one decision table, not two.** `amplifyIm1BuildImportPlan_` is a near-verbatim, drift-checked port of `scripts/import-staging/build-import-plan.mjs`'s `buildImportPlan` (Apps Script has no ES module system, so a literal `import` is impossible). `scripts/import-staging/importer.test.mjs` runs **both** implementations against the **same** fixtures (the full 164-item real artifact against both an empty destination and `fixtures/representative-destination.json`) and asserts byte-identical JSON output — the two cannot silently diverge without failing a test. A real bug was caught this way during development: writing `null` to a sheet cell reads back as `""`, which the original comparison logic treated as a genuine content difference, causing every rerun to spuriously re-propose the same "update" forever. Fixed identically in both files via a shared `normalizeBlankToNull`/`amplifyIm1NormalizeBlankToNull_` helper — see both files' inline comments.

**Locking.** `LockService.getScriptLock()`, 30-second `tryLock` timeout, released in a `finally` block, never held during `previewAmplifyIm1Import()`. Lock acquisition is injectable (`deps.lockService`) so `importer.test.mjs` can simulate a failed acquisition without any real concurrency.

**Backup design.** Mirrors `ProductionDataCleanup.js`'s `cleanupCreateBackup_` exactly: `Spreadsheet.copy(name)` — a complete, independent copy as a new Drive file, never a mutation of the original. Named `Year Planner Database — pre-amplify-im1-import <timestamp>`; the returned record includes the backup's ID/URL/name plus the artifact hash and confirmation string that triggered it. Backup creation is injectable via `deps.formatTimestamp` for the same testability reason as locking. Backups are never deleted automatically, and no automatic restore/rollback exists.

**Guarded write sequence** (mirrors `executeProductionDataCleanupV1Locked_`'s ordering exactly): (1) validate confirmation, (2) validate embedded payload (hash self-consistency + structural collision/ordering checks), (3) acquire lock, (4) validate destination schema, (5) read current state and build the planning-pass plan, (6) refuse a blocked plan, (7) create backup, (8) re-read current state and rebuild the plan (revalidation pass), (9) abort if the planning-pass and revalidation-pass plans differ (state changed underneath the operation — the same technique `ProductionDataCleanup.js` uses, adapted), (10–11) apply only `create`/`source-update` actions via field-level writes (never a whole-row rewrite) for updates and one batched `setValues` append for new rows, using each sheet's actual current header order rather than assumed fixed positions, (12) post-write verification, (13) release the lock in `finally` and return a structured report regardless of outcome. A partial-write is reported honestly: `writesOccurred` is only ever set `true` after the write step returns without throwing, and if an exception occurs after a backup already exists, the error message explicitly says manual recovery may be required from that backup rather than implying safety by omission.

**Create behavior.** New rows never populate `PlannedDays`/`TeacherNotes`/`PrimaryLink`/`LessonNumber`; fixed items get their artifact `order` as `SortOrder`, flexible items get a blank `SortOrder` and their `PlacementRule`; literal `Type` is preserved exactly; header order is read dynamically from the live sheet, never assumed.

**Source-update behavior.** Only the specific publisher-owned fields that actually differ are written, cell by cell — never a whole-row rewrite. A row with any populated teacher-owned field is `blocked` instead, with `reasons: ["preserve-teacher-fields"]` and the specific diffs listed, never silently resolved toward either side.

**Idempotence — proven in simulation, not asserted.** `importer.test.mjs` runs the full guarded sequence against a fake, empty destination and confirms: first run creates exactly 1 unit / 2 items (the minimal test payload) and the real 164-item artifact's parity test confirms 7 units / 164 items would be created against a real empty destination; a second run against the resulting state produces all no-ops (zero creates, zero updates); a source-owned field change made directly on the fake sheet after the first run produces exactly one narrowly-scoped `source-update` on rerun; an unrelated legacy Math 8 row is provably byte-identical before and after; teacher-owned fields populated between runs survive a rerun even when a publisher field also changed on the same row (the row is `blocked`, not partially merged); flexible items never receive a fixed `SortOrder`; `LessonNumber` is never fabricated on create and is never touched on update.

**Post-write verification — reuses the plan builder, does not re-implement it.** `amplifyIm1VerifyAgainstPayload_` classifies the current state against the payload using the exact same `amplifyIm1BuildImportPlan_`: any item still `create` is reported missing (error); any item `source-update` means an intended write did not take effect (error — this should never happen after a successful execute); any item `blocked` for `preserve-teacher-fields` is counted as `knownStaleCount`, not an error, because that is the correct and intentional outcome of protecting teacher-authored data; any other `blocked` reason (duplicate/cross-course collision) is an error. This deliberately avoids a second, hand-written field-comparison engine that could drift from the plan builder's own rules. `verifyAmplifyIm1Import()` is the read-only, standalone operator entry point — it has no memory of what a specific execute run touched, so it evaluates the entire current state against the payload fresh, every time it's called.

**Simulation architecture.** `scripts/import-staging/fake-spreadsheet.mjs` implements the minimal in-memory `FakeSpreadsheet`/`FakeSheet`/`FakeRange` surface the importer actually calls (`getSheetByName`, `getLastRow`/`getLastColumn`, `getRange().getValues()/.setValues()/.setValue()`, `appendRow`, `deleteRow`, `copy`) plus injectable fake `LockService`. Nothing here is a real Google API client; nothing here can reach a real spreadsheet even by accident, because the fakes simply don't implement network calls.

**Test results.** `node --test scripts/import-staging/importer.test.mjs` — **32/32 pass**, covering: two full-artifact parity tests (empty and representative-fixture destinations, Apps Script plan builder vs. Node plan builder, byte-identical JSON); the confirmation contract (exact/stale/whitespace/boolean/generic-string rejection); payload integrity (hash tamper, unsupported schema version) and structure (duplicate IDs, both/neither order-and-placementRule); destination schema validation (including the real current-production-shape Lessons headers correctly failing on missing `Type`/`PlacementRule`); post-write verification (pass, catch-missing/duplicate/reverted-write, known-stale-is-not-an-error); the full guarded execute sequence's every documented refusal path (wrong confirmation, unsupported schema version, tampered hash, missing sheet, missing header, duplicate destination ID, cross-course collision, lock-acquisition failure, backup failure, state-changed-between-passes); a full successful create run with verification; the complete idempotence matrix above; and a mid-write exception reported honestly rather than silently. Sprint 3's own 24 tests were re-run and still pass (`build-import-plan.mjs`'s blank-normalization fix was applied identically to both the Node and Apps Script plan builders and re-verified).

**Safety failures proven** (Task 18's list, all exercised — see the test names above for exact mapping): wrong/stale confirmation, tampered artifact hash, unsupported schema version, missing required sheet, missing required header, duplicate destination ID, cross-course ID collision, blocked action, backup failure, lock-acquisition failure, post-write verification failure (proven directly against the pure verification function with a deliberately corrupted current-state fixture), state changed between planning and revalidation passes, flexible item with a fixed SortOrder, fixed item lacking SortOrder (both via payload-structure validation).

**Known limitations and unverified production assumptions.**
- **Never run against production.** Every guarantee above is a locally-proven, fixture-based guarantee. Nothing in this sprint confirms the real `getSheetData()`/`SpreadsheetApp` behavior matches the fakes in every respect (e.g., real Sheets API quirks around type coercion, locale-specific number/date formatting, or very large batch writes) — only that the importer's own logic is internally consistent and behaves correctly against a faithful model of the documented schema.
- **The production Lessons sheet does not yet have `Type`/`PlacementRule` columns.** Schema validation will correctly report `blocked` until a separate, explicit schema-migration step adds them — this importer does not add columns itself, by design (§6 of this sprint's task list: "do not silently add or rename production columns during import execution").
- **`LessonNumber` is never populated.** A future extraction refinement would need to add a genuine, source-derived `LessonNumber` field to the artifact contract before this gap can close without guessing.
- **No menu integration.** `previewAmplifyIm1Import()`/`executeAmplifyIm1Import()`/`verifyAmplifyIm1Import()` are callable only by name (Apps Script editor "Run" with an explicit argument, or a future disposable wrapper) — no `onOpen` menu item was added this sprint, to avoid adding a low-friction execution surface.
- **Apps Script's SHA-256 (`Utilities.computeDigest`) parity with Node's `crypto` was reasoned about, not independently cross-verified inside the real Apps Script runtime** — both are standard SHA-256 over the same UTF-8 bytes, so this should match, but it has not been executed inside an actual Apps Script project this sprint.

**Remaining prerequisites before a read-only production preview:**
1. Confirm (read-only, e.g. via `ProductionDataAudit.js`'s existing pattern) the actual current header row of the production Units/Lessons sheets — do not assume they still exactly match `SHEET_STRUCTURE.md`.
2. Decide and execute the `Type`/`PlacementRule` column-addition migration on the production Lessons sheet (an explicit, separate, reviewed step — not something `previewAmplifyIm1Import()` should ever do itself).
3. Deploy `AmplifyIm1Importer.js` and `AmplifyIm1ImportData.js` to the `apps-script-planning` Apps Script project via `clasp push` (not done this sprint — no deployment occurred).
4. Run `previewAmplifyIm1Import()` for real and manually review its report before any future sprint is authorized to run `executeAmplifyIm1Import()`.

**Exact future production-run procedure** (once the above prerequisites are met and a later task explicitly authorizes it): run `previewAmplifyIm1Import()` from the Apps Script editor, review the full report (especially `plan.blocked` and every `blocked`/`title-mismatch-warning` entry), copy `confirmationRequired` exactly, then call `executeAmplifyIm1Import(<that exact string>)` — never with a hand-typed approximation. Review the returned report's `verification.valid` and `errorStage` before considering the import complete; if `errorStage` is non-null, treat the backup recorded in the report as the authoritative recovery path and do not re-run until the cause is understood.

---

## Sprint 5 — Read-Only Production Audit

**Current status, stated precisely:** The production schema and data shape have been audited read-only. The importer requires a `UnitTitle` mapping correction and a Lessons `Type`/`PlacementRule` schema migration before a real importer preview can run. No production execution is authorized.

**Repository and Apps Script identity.** Audited from repository checkpoint `1c7c9bdf08d33a998508eade6899008ec4701b8a` (Sprint 4's commit). Apps Script account: `clasp show-authorized-user` confirmed `jholcomb@scottsvalleyusd.org`. Targeted project confirmed as the one the live Planning frontend actually uses: `clasp list-deployments`' `@26` deployment ID matches the URL hard-coded in `frontend/src/api.js` exactly.

**Scope: verification only.** No write, backup, deployment, or `executeAmplifyIm1Import()` call occurred. Real production data below was read entirely through the already-deployed, already-anonymous, already-read-only `doGet` endpoint (Code.js) — the exact same request the frontend already makes on every page load — not through Apps Script function execution. `clasp run` was attempted once, against the pre-existing, harmless `showSpreadsheetUrl()` function, and failed with "Script function not found. Please make sure script is deployed as API executable." This project has no API-Executable deployment (only two web-app deployments: `@HEAD` and the pinned `@26` that the real frontend actually calls), and creating one would itself be a new deployment — out of this sprint's authorized scope. No browser was available in this environment to invoke editor functions manually either. **No Apps Script function was ever executed this sprint; `previewAmplifyIm1Import()` and `verifyAmplifyIm1Import()` were not run for real.** Every preview-classification statement below (what *would* block, what *would* classify as `create`) is **inferred** from the schema/data facts read through `doGet`, not **observed** by actually running the importer. `clasp push` was used once to bring `Code.js`, `AmplifyIm1Importer.js`, and `AmplifyIm1ImportData.js` up to date on the project's HEAD source — confirmed harmless because the live frontend's web-app URL resolves to the pinned `@26` deployment, not `@HEAD`; pushing changes HEAD source only and does not affect what the classroom-facing app currently serves. No new deployment or version was created (`list-deployments` before and after: identical, same two deployment IDs).

**Real Units headers (18 rows):** `UnitID, CourseID, UnitNumber, UnitTitle, RequiredDays, OptionalDays, SortOrder, UnitPurpose` — uniform across all 18 rows, no blank or duplicate header names detected (derived from the `doGet` JSON's object keys, which reconstruct header order faithfully but cannot detect a duplicate header that `getSheetData()` itself would have already silently collapsed).

**Real Lessons headers (55 rows):** `LessonID, UnitID, CourseID, LessonNumber, LessonTitle, PlannedDays, SortOrder, KeyOutcome, Description, PrimaryLink, TeacherNotes, IsOptional` — matches `SHEET_STRUCTURE.md` and this importer's assumptions exactly. `Type` and `PlacementRule` confirmed absent on every row (not merely assumed).

**Critical finding — Units schema mismatch.** The importer's `AMPLIFY_IM1_REQUIRED_UNIT_HEADERS` requires `UnitName`; **the real column is named `UnitTitle`.** `UnitName` does not exist anywhere in the real Units sheet. Two consequences: (1) `amplifyIm1ValidateDestinationSchema_` will correctly report Units `blocked` against real production today — for a different, previously-undocumented reason than the already-known Lessons `Type`/`PlacementRule` gap; (2) even if the header name were corrected, `amplifyIm1PlanUnit_`/`planUnit`'s `destinationUnit.UnitName !== artifactUnit.title` comparison reads a field that doesn't exist on a real row (`undefined`), which the fail-closed schema gate prevents from ever being reached in practice — but the mapping itself is still wrong and must be corrected before this importer's Units logic can be trusted, schema gate or not. Additionally, the real sheet has an actual `UnitNumber` column (numeric, populated on every row) that neither `SHEET_STRUCTURE.md` nor the importer's schema/mapping account for at all — this contradicts this project's own prior assumption (stated in `scripts/import-staging/README.md` and this importer's header comment) that "no `UnitNumber` column exists in Units, only embedded in `UnitID`." That assumption was wrong; it must be corrected in code comments and in the eventual schema-mapping fix, not carried forward silently.

**Real Units data (18 rows: 9 Math 8, 9 IM1).** No duplicate or blank `UnitID`s. `RequiredDays`/`OptionalDays` are populated, numeric, non-blank on every row (including all 9 IM1 rows) — real teacher-entered planning data already exists for the current IM1 placeholder units and must never be touched by any future write. The 9 real IM1 `UnitID`s are `IM1-U0` through `IM1-U8` (an `IM1-U0` "Class Orientation" unit exists that the extraction never covered) — titles confirm Extraction Note #1's warning was correct: real `IM1-U5` UnitTitle is "Systems of Linear Equations and Inequalities" (Amplify's actual Unit 3), and real `IM1-U2` UnitTitle is "Linear Equations and Inequalities" (at the time of this Sprint 5 audit, believed close to but not an exact match for Amplify's actual Unit 2 title, then recorded as "Solving Equations and Inequalities" — **corrected in Sprint 6.3D's read-only production-preview audit: the extraction's title was the error, and this real `IM1-U2` placeholder title was an exact match all along; see `IM1_Curriculum_Extraction.md`'s Extraction Note #19**). None of this matters to the importer's own matching (which is ID-based, using the deliberately distinct `AMP-IM1-U{n}` scheme, confirmed to have **zero** overlap with any real `UnitID` or `LessonID`), but it confirms title-based reconciliation would have been unsafe, validating that design choice. More generally: existing placeholder `UnitID` numbering (`IM1-U0`…`IM1-U8`) and Amplify's own publisher unit numbering (`M1.1`…`M1.7`, as the extraction records it) **do not correspond one-to-one** — matching must stay ID-based against the staged artifact's own IDs, never inferred from either numbering scheme or from title similarity. Unrelated Math 8 rows (`M8-U0`…`M8-U8`, 34 lesson rows) were read via the same `doGet` call as part of the whole-workbook response, but were not otherwise inspected or targeted — they fall entirely outside the `AMP-IM1-*`/`IM1-*` ID space the importer matches against and must remain untouched by any future write.

**Real Lessons data (55 rows).** No duplicate or blank `LessonID`s. Zero rows have any `TeacherNotes` or `PrimaryLink` content — meaning the importer's teacher-field-protection logic, while proven correct in local simulation, **has never yet been exercised against a real populated teacher-owned field**; this remains an unverified-in-practice gap, not a resolved one. `IsOptional` is stored as an empty string (`""`, JS `typeof "string"`) on every existing row, never as a genuine boolean in current data — `Boolean("")` correctly evaluates `false`, so today's data poses no problem, but this means the sheet's actual boolean-storage behavior (string vs. real boolean cell) has not been observed for a `true` value and should not be assumed either way before a real write. `SortOrder`/`LessonNumber`/`PlannedDays` are all genuine JS `number` values, confirming the importer's numeric-comparison assumptions. 5 real IM1 lesson rows exist (`IM1-U1-L1`, `IM1-U1-L2` under the real "Patterns and Sequences" unit, plus `IM1-U0-L1..L3` under "Class Orientation") — all outside the `AMP-IM1-` ID namespace, confirmed zero collision.

**ID collision result:** zero overlap between any real `UnitID`/`LessonID` and any staged `AMP-IM1-U{n}`/`AMP-IM1-U{n}-I{order}`/`AMP-IM1-U{n}-F{index}` ID — every staged unit/item would classify `create` against the real destination today, blocked only by the two schema gaps above (Lessons `Type`/`PlacementRule`, Units `UnitName`/`UnitTitle`), not by any ID conflict.

**Required importer code corrections identified (not made this sprint — read-only audit only):**
1. `AMPLIFY_IM1_REQUIRED_UNIT_HEADERS`: replace `UnitName` with `UnitTitle`.
2. `amplifyIm1PlanUnit_`/`planUnit` (both files): read/write `UnitTitle`, not `UnitName`, everywhere a Unit's title is compared or proposed.
3. Decide whether to also read/compare the real `UnitNumber` column (currently unused by the importer entirely) — the artifact already carries `unitNumber`; whether it's safe/desirable to compare or backfill it against the real column is a product decision for the migration-design sprint, not resolved here.
4. Corresponding fix to `scripts/import-staging/README.md`'s and `AmplifyIm1Importer.js`'s header comments, which both currently state the incorrect "no `UnitNumber` column exists" assumption.

**Schema migration required (updated):** adding `Type` and `PlacementRule` to Lessons remains necessary and sufficient for Lessons. Units requires no *new* column — `UnitTitle` and `UnitNumber` already exist — only an importer *code* correction (item 1–2 above), not a spreadsheet migration. This narrows the originally-assumed migration scope: Units needs a code fix, not a schema change; only Lessons needs an actual column-addition migration. The Lessons migration and the Units importer correction should be **reviewed together** in the next sprint (both stem from this same audit and both gate the same eventual preview run) but **implemented as two separate, explicit operations** — a spreadsheet schema change and a code change are different kinds of risk and should not be bundled into one commit or one review.

**Independent no-mutation verification:** `doGet` was called twice — once before `clasp push`, once after — both within the same audit session; `units.length`, `lessons.length`, and the full JSON-serialized content of both arrays were byte-identical between the two reads. `list-deployments` was identical before and after `clasp push` (same two deployment IDs, no new entry). `executeAmplifyIm1Import()` was never referenced in any command run this sprint.

**Decision-gate answers (see the audit task's report for full context):** the real Apps Script runtime's `Utilities.computeDigest()` SHA-256 was **not** exercised this sprint (no execution path was available) — parity remains reasoned-about, not measured. The real production schema does **not** fully match importer expectations (Units field-name mismatch, confirmed). A migration is required, but only for Lessons (`Type`/`PlacementRule`); Units needs an importer code fix instead. The real preview was never run, so "blocked actions beyond missing schema fields" could not be observed directly — the schema-validation gate alone would already block Units and Lessons for the reasons above. Importer field mappings are **not** fully accurate for the real Units sheet (Lessons mapping is accurate). The read-only audit produced zero spreadsheet mutations (independently verified). The project is ready for a separate, narrowly-scoped migration/code-correction sprint — schema migration for Lessons, code correction for Units — but not for a real preview run until both are addressed and an execution path (a human running it in the browser editor, or an explicitly-authorized API-Executable deployment) exists.

---

## Sprint 6.1 — Units Importer Code Correction

**Scope: code only.** No spreadsheet changes, no Apps Script execution, no deployment, no Lessons schema changes. This sprint implements exactly the two Units-side corrections the Sprint 5 audit identified (items 1–2 of its "Required importer code corrections identified" list) and the corresponding documentation fixes (item 4). The Lessons `Type`/`PlacementRule` column-addition migration (item 3 of that list, and the audit's separately-tracked schema migration) remains explicitly out of scope for this sprint, per the audit's own recommendation to implement the two corrections as separate, explicit operations.

**UnitTitle correction.** `AMPLIFY_IM1_REQUIRED_UNIT_HEADERS` (`apps-script-planning/AmplifyIm1Importer.js`) no longer requires `UnitName`; it requires `UnitTitle`, matching the real production column confirmed by the Sprint 5 audit. `amplifyIm1PlanUnit_`/`planUnit` (both `AmplifyIm1Importer.js` and `scripts/import-staging/build-import-plan.mjs`) now read and propose `UnitTitle` everywhere a Unit's title is compared or written — on `create` (`proposedRow.UnitTitle`) and on `source-update` (a `UnitTitle` publisher-field diff, still carrying the `title-mismatch-warning` reason exactly as the old `UnitName` diff did).

**UnitNumber decision: publisher-owned, included in comparison.** The audit established `UnitNumber` is a real, populated production column, previously entirely unused by the importer. Reviewed against `CURRICULUM_INFORMATION_MODEL.md`'s ownership model: `UnitNumber` is Amplify's own sequencing number for the unit (the artifact already carries it as `unitNumber`, sourced from the extraction, the same origin as `Title`) — it is not a value a teacher enters or edits, so it fits the existing publisher-owned category (alongside `UnitTitle`, item `Title`/`Type`/`SortOrder`/`PlacementRule`/`IsOptional`) with no architectural conflict found. No documented decision (D-1 through D-5) assigns `UnitNumber` elsewhere. Implemented as: `AMPLIFY_IM1_REQUIRED_UNIT_HEADERS` now also requires `UnitNumber`; a new `amplifyIm1UnitPublisherFieldsDiffer_`/`unitPublisherFieldsDiffer` helper (mirroring the existing item-level `amplifyIm1PublisherFieldsDiffer_`/`publisherFieldsDiffer` pattern) compares both `UnitTitle` and `UnitNumber` against the artifact and returns a `publisherFieldDiffs` array; a `create` row now populates `UnitNumber: artifactUnit.unitNumber` in addition to the pre-existing `SortOrder: artifactUnit.unitNumber` (the two remain separate destination columns fed from the same artifact source value — `SortOrder` is physical position, `UnitNumber` is Amplify's own labeling number; they happen to coincide for this artifact because units are extracted in Amplify's own order, but nothing about this change assumes they must always coincide). A `UnitNumber`-only mismatch now classifies `source-update` without the `title-mismatch-warning` reason (that reason remains specific to an actual title mismatch). This generalizes the previous single-field (`UnitName`/`UnitTitle`-only) unit diff into a small array-based diff, matching the item-level design already in place — no other unit-level behavior changed: `RequiredDays`/`OptionalDays` remain teacher-owned and are still never proposed for update once a Unit row exists (D-2/D-5), and duplicate-ID / cross-course-ID hard-fail handling is unchanged.

**Documentation corrections made this sprint:** `docs/Reference/SHEET_STRUCTURE.md`'s Units field list now reads `UnitID, CourseID, UnitNumber, UnitTitle, RequiredDays, OptionalDays, SortOrder` (was `UnitID, CourseID, UnitName, RequiredDays, OptionalDays, SortOrder`). `scripts/import-staging/README.md`'s field-ownership section now lists `UnitTitle`/`UnitNumber` as publisher-owned instead of `UnitName`, with a note on the correction. Test fixtures and unit tests that constructed destination Unit rows with `UnitName` (`scripts/import-staging/test.mjs`, `scripts/import-staging/importer.test.mjs`, `scripts/import-staging/fixtures/representative-destination.json`) were updated to `UnitTitle`/`UnitNumber` so intentional exact-match/no-op and title-mismatch/source-update test scenarios still exercise the branch they were designed to exercise, rather than incidentally becoming source-updates because of the field rename.

**Checkpoint-review additions (same sprint, before commit):** the pre-existing fixture and test set proved the `UnitTitle` path but left the new `UnitNumber` path (and the real `IM1-*`-vs-`AMP-IM1-*` ID-collision guarantee) unexercised. `scripts/import-staging/fixtures/representative-destination.json` gained three rows: `AMP-IM1-U4` (title matches, `UnitNumber` deliberately wrong — proves a `UnitNumber`-only diff), `AMP-IM1-U5` (both title and `UnitNumber` wrong — proves a combined diff still carries exactly one `title-mismatch-warning`, not two reasons), and `IM1-U1` (a genuine placeholder-shaped unit, real `UnitID` namespace, populated `RequiredDays`/`OptionalDays`/`TeacherNotes`, paired with an `IM1-U1-L1` lesson row) that the artifact's `AMP-IM1-*` IDs must never look up or reference. `scripts/import-staging/test.mjs` gained four tests exercising exactly these rows: `UnitNumber`-only source-update (no title-mismatch reason), combined UnitTitle+UnitNumber source-update (both diffs, one reason), an explicit unit-level no-op assertion added to the existing exact-match test, and an exact-ID-lookup (not substring) proof that `IM1-U1`/`IM1-U1-L1` never appear anywhere in the plan. The existing title-mismatch test was tightened to assert the diff set is exactly `["UnitTitle"]`, not merely "does not include RequiredDays/OptionalDays." No new fixture scenario was added to `importer.test.mjs` directly — its two parity tests already re-run the same extended fixture through both plan builders and assert byte-identical JSON, which is the Apps-Script-side proof for all three new rows without duplicating the same decision-table assertions in two files.

**Validated this sprint (including the checkpoint review):** Sprint 3's `test.mjs` — **27/27 pass** (24 original + 3 added this review pass; see above). Sprint 4's `importer.test.mjs` — **32/32 pass**, both parity tests (empty destination and the now-extended representative-fixture destination) still assert byte-identical JSON between the Node and Apps Script plan builders. `generate-artifact.mjs --check` and `generate-apps-script-payload.mjs --check` both pass with zero drift (this sprint touched importer/plan-builder/test logic only, never the artifact, its generator, or the embedded payload). Both `preview.mjs` CLI runs (empty destination, representative destination) complete locally against fixtures with "No data was written" and the expected classification counts. `node --check` passes on every modified `.js`/`.mjs` file; `git diff --check` reports no whitespace errors.

**Still not done — unchanged from Sprint 5's conclusion:** the Lessons `Type`/`PlacementRule` schema migration remains outstanding and is a separate, explicit, reviewed spreadsheet operation, not part of this sprint. No `clasp push`, no Apps Script version/deployment, and no production spreadsheet read or write occurred this sprint. A real `previewAmplifyIm1Import()` run remains blocked until the Lessons migration lands and an execution path exists (see Sprint 4's "Remaining prerequisites" and Sprint 5's conclusion, both still current).

---

## Sprint 6.2A — Lessons Schema Migration (Type / PlacementRule)

**Scope: code only, in this sprint.** Builds the guarded Apps Script migration that adds `Type` and `PlacementRule` to the production `Lessons` sheet — the schema-migration half of Sprint 5's audit findings, deliberately implemented as a separate, explicit operation from the Sprint 6.1 Units code correction (a spreadsheet schema change and a code change are different kinds of risk). **Not executed against production this sprint.** No Apps Script deployment, no `clasp push`, no production read or write, no curriculum row added, no `previewAmplifyIm1Import()`/`executeAmplifyIm1Import()` call.

**Current status, stated precisely:** ready for a controlled read-only production preview after checkpointing; not yet authorized for production execution.

**Migration module.** `apps-script-planning/LessonsSchemaMigration.js` — a dedicated file, independently reviewable and removable, never folded into `Code.js` or `AmplifyIm1Importer.js`. Same two-section shape as that importer (pure logic under `module.exports`, testable directly under Node; an Apps Script I/O boundary below it). Public functions: `previewLessonsTypePlacementRuleMigration()` (read-only), `executeLessonsTypePlacementRuleMigration(confirmation)` (guarded write; no default parameter, so a zero-argument call always refuses), `verifyLessonsTypePlacementRuleMigration()` (read-only, standalone, no memory of a specific execute run — mirrors `verifyAmplifyIm1Import()`'s same honest limitation).

**Consumer positional-assumption review (required before implementation, per this sprint's task).** Searched every Apps Script reader/writer of `Lessons` (`Code.js`'s `getSheetData`, `addLesson`, `updateLesson`, `deleteLesson`, `reorderLessons`) and every frontend consumer. Result: **no fixed-column-index risk found.** Every Apps Script function resolves columns via `headers.indexOf("HeaderName")` at call time and writes/reads via `headers.length`-sized ranges — never a hardcoded numeric position or an `A1:L`-style fixed range. `getSheetData()` maps every row to a header-keyed object dynamically; the frontend consumes only those JSON objects by property name (confirmed by grep across `frontend/src` — every `KeyOutcome`/`PrimaryLink`/`TeacherNotes`/`IsOptional` reference is a `.PropertyName` object access, never an array index). The only positional-array code found anywhere in the repository (`apps-script-roster-admin/RosterImport.js`, `row[0]`..`row[3]`) belongs to a completely unrelated sheet (roster CSV staging) in a separate Apps Script project and cannot be affected. This cleared the task's explicit gate to proceed with implementation rather than stopping to report a blocking risk.

**Header placement.** `Type` and `PlacementRule` are inserted immediately after `SortOrder` (position 7 of 12), producing the approved final order: `LessonID, UnitID, CourseID, LessonNumber, LessonTitle, PlannedDays, SortOrder, Type, PlacementRule, KeyOutcome, Description, PrimaryLink, TeacherNotes, IsOptional`. This groups the new sequencing/type fields with the other structural fields rather than appending them at the far right, and is safe precisely because of the consumer review above — nothing depends on `KeyOutcome` (or anything after it) being at a fixed column letter.

**Schema-state classification — one function, four states.** `lessonsMigrationClassifySchema_(headers)` is the single source of truth: any duplicate or blank header anywhere in the active header row is `unexpected` immediately (regardless of `Type`/`PlacementRule` state); exactly one of `Type`/`PlacementRule` present is `partially-complete` (refused — never guesses how the partial state occurred or which column to add); both present is `already-complete` only if the full header array exactly equals the approved final array, otherwise `unexpected`; neither present is `migration-required` only if the full header array exactly equals the audited original array, otherwise `unexpected`. Exact full-array equality (never a subset/fuzzy match) is what makes "unknown columns interleaved" and "header order differs materially" both collapse safely into the same `unexpected` refusal without needing separate ad hoc detection for each.

**Confirmation contract.** `MIGRATE_LESSONS_SCHEMA_ADD_TYPE_AND_PLACEMENTRULE_V1` — exact `===` match, no trim, no case-folding, no boolean/truthy acceptance. Chosen over a row-count-suffixed form specifically because the row count can legitimately change before this migration is ever authorized to run; deriving the phrase from a value that can drift would either go stale for no real reason or invite hand-editing it to match. Not treated as a secret — shown directly in the preview report.

**Locking and backup.** `LockService.getScriptLock()`, 30-second `tryLock` timeout, released in `finally` (wrapped in its own try/catch as of Sprint 6.2B, below), never held during preview. `Spreadsheet.copy(name)` — mirrors `amplifyIm1CreateBackup_`/`cleanupCreateBackup_` exactly, named `Year Planner Database — pre-lessons-type-placementrule-migration <timestamp>`, recording the source spreadsheet ID directly on the backup object itself (as of Sprint 6.2B, not just alongside it in the outer report), backup ID/URL/name, original Lessons row count, column count, and full header array, plus the confirmation string that triggered it. Never deleted automatically.

**Guarded write sequence** (mirrors `executeProductionDataCleanupV1Locked_`/`amplifyIm1ExecuteLocked_`'s ordering, adapted for a schema change instead of a row-level diff): (1) validate confirmation, (2) acquire lock, (3) planning pass — reread and classify the current schema under lock, never trusting a prior preview; `already-complete` short-circuits here as a successful no-op (`writesOccurred: false`, no backup), anything other than `migration-required` refuses with `errorStage: "schema"`, (4) create backup (metadata sourced from the planning-pass read), (5) revalidation pass — reread and re-classify immediately before mutating; abort (`errorStage: "revalidation"`) if headers or row count changed since the planning pass, (6) snapshot the full pre-migration state for verification purposes (all 12 original fields, every row, by header name) — built from the **revalidation-pass** read, not the planning-pass read (Sprint 6.2B correction, below), (7) one `insertColumnsAfter(7, 2)` call followed by one header-row write of `["Type", "PlacementRule"]` — never a whole-sheet rewrite; `writesOccurred`/`columnsInserted` are set immediately once this step returns, *before* the subsequent `flush()` call (Sprint 6.2B correction, below), (8) post-write verification against the pre-migration snapshot, (9) release the lock in `finally` and return a structured report regardless of outcome.

**Post-write verification.** Compares the pre-migration snapshot against post-migration state: final headers must exactly equal the approved array; column count must have increased by exactly 2; row count must be unchanged; every original field's value, for every row, looked up by header name, must equal its pre-migration value exactly (`JSON.stringify` equality of the two snapshots — the same pattern `amplifyIm1PlansEqual_`/`cleanupPlansMatch_` already use elsewhere in this codebase); both new columns must be blank on every row. Any mismatch makes the run report `errorStage: "post-write-verification"` and directs the operator to the backup — never a silent success. No automatic rollback is implemented anywhere in this file.

**Idempotence — proven in simulation.** A first run against the audited original schema creates the two columns and returns success; a second run against the resulting state classifies `already-complete` and returns a successful no-op — `writesOccurred: false`, `columnsInserted: false`, `backup: null` (no second backup is ever created).

**Simulation layer.** `scripts/import-staging/fake-spreadsheet.mjs` gained `FakeSheet.insertColumnsAfter(afterPosition, howMany)` (splices blank cells into every row at the given position, mirroring the real `Sheet.insertColumnsAfter` contract) and `createFakeSpreadsheetFromRawSheets(sheetsByNameAndRows)` (a raw header/row-array constructor, since this migration reasons about headers and raw rows directly, never field-keyed objects). Backup-failure and lock-failure simulation reuse the exact techniques already established in `importer.test.mjs` (overriding `spreadsheet.copy` to throw; `createFakeLockService({ acquireSucceeds: false })`) — no new simulation primitive was needed for either.

**Test results (as originally implemented; superseded by Sprint 6.2B's 29/29 below).** `node --test scripts/import-staging/lessons-schema-migration.test.mjs` — 18/18 pass, covering: preview correctness and zero-write proof; the full confirmation contract (missing/wrong/whitespace-padded); every schema-precondition refusal (missing sheet, missing original header, duplicate header, blank header, `Type`-only, `PlacementRule`-only, unexpected order); lock-failure and backup-failure refusals (both proven to leave the sheet untouched); a full valid execute run proving exactly two columns inserted, approved final order, unchanged row count, every original field (including `LessonID`/`UnitID`) still attached to its original header name, both new columns blank on every row, verification passing, and the `Units` sheet byte-identical before and after; the idempotent no-op rerun (no second backup); a post-write verification failure via simulated data corruption; and a mid-migration failure between column insertion and the header write.

**Known limitations and unverified production assumptions** (same category as Sprint 4's own list, restated for this migration specifically): never run against production — every guarantee above is a locally-proven, fixture-based guarantee; real Sheets API behavior for `insertColumnsAfter` (interaction with existing formatting, notes, data validation, frozen rows, or any in-sheet formula) has been reasoned about, not observed, since the fakes model only cell values; Apps Script deployment has not occurred for this file (`clasp push` was not run this sprint, per this sprint's explicit constraints).

**Remaining prerequisites before a real production migration:**
1. Deploy `LessonsSchemaMigration.js` to the `apps-script-planning` Apps Script project via `clasp push` (not done this sprint — no deployment occurred; the live frontend remains pinned to deployment `@26` regardless of what lands on `HEAD`).
2. Run `previewLessonsTypePlacementRuleMigration()` for real and manually review its report — especially `classification.state`, which must read `"migration-required"` against the real 55-row Lessons sheet — before any future sprint is authorized to run `executeLessonsTypePlacementRuleMigration()`.
3. Once the migration itself has run and been verified, the Sprint 6.1 Units correction and this migration together satisfy Sprint 4's "Remaining prerequisites" items 1–2; a real `previewAmplifyIm1Import()` run remains the next, separately authorized step after that — still no curriculum import in this sprint or the one after it.

---

## Sprint 6.2B — Adversarial Safety Review of the Lessons Schema Migration

**Scope: review and narrow correction of Sprint 6.2A's uncommitted implementation, before any commit.** No production execution, no deployment, no `clasp push`, no importer/Units/frontend changes. The explicit brief: do not assume the implementation is correct because its tests passed — actively try to find ways it could corrupt data, mutate more than intended, create an unusable partial state, report success incorrectly, fail to detect a production incompatibility, create a misleading backup, or behave differently in real Apps Script than in the local fake.

**Corrections made (all narrow, all in `LessonsSchemaMigration.js` unless noted):**

1. **Mutation-flag timing (highest-severity finding).** `writesOccurred`/`columnsInserted`/`writeDetails` were previously set *after* the `SpreadsheetApp.flush()` call. `flush()` can throw (quota, a transient API error) without undoing a mutation that already fully completed via `insertColumnsAfter`/`setValues` — under the old ordering, a flush failure would have caused the report to falsely claim `writesOccurred: false` for a migration that had, in fact, already succeeded structurally. Fixed by moving the flag-setting to immediately after the insert-and-header-write step returns, before `flush()` is even called. Proven by a new test that makes `flush()` throw after a fully successful mutation and confirms the report still says `columnsInserted: true, writesOccurred: true`.
2. **Post-write verification snapshot window.** The "before" snapshot used for post-write comparison was built from the *planning-pass* read, taken well before the backup and the revalidation pass. Because `Code.js`'s `addLesson`/`updateLesson`/`deleteLesson`/`reorderLessons` — the live classroom frontend's actual write paths — acquire no lock at all (confirmed by inspection, not assumed), a legitimate concurrent classroom edit landing between the planning pass and the mutation could cause a false-positive verification failure, misattributing an ordinary concurrent edit to migration corruption. Fixed by rebuilding the snapshot from the *revalidation-pass* read (already performed, previously discarded) instead — the freshest read available immediately before the mutating call, narrowing the vulnerable window as far as achievable without modifying `Code.js`'s write paths, which is out of this file's scope.
3. **Lock-release robustness.** `lock.releaseLock()` in the `finally` block was unguarded — if it threw, it would override/swallow the already-computed `report` object (which may carry a critical backup ID) with an uncaught exception instead of returning it to the caller. Fixed by wrapping `releaseLock()` in its own try/catch, swallowing only the release error, never the report. (Note: `ProductionDataCleanup.js` and `AmplifyIm1Importer.js` share this same unguarded pattern; fixing it there is out of this task's scope, which is limited to the Lessons migration.)
4. **Backup self-containedness.** `lessonsMigrationCreateBackup_`'s returned object did not carry the source spreadsheet ID itself (only the outer report did, alongside it). Added `sourceSpreadsheetId` directly to the backup record so source and backup identities cannot be confused even if `report.backup` is copied out of its surrounding context.

**Findings addressed by documentation rather than code** (per the review's own instruction: "add a preflight check or document an explicit operator prerequisite" — documentation was judged the narrower, sufficient response given the code already fails closed via its exception-handling path in every case below):
- **Real Sheets behavior unverifiable locally:** filters, filter views, protected ranges/sheets, merged cells, named ranges, conditional formatting, notes, comments, charts, and pivot tables anywhere touching Lessons are not something this migration's code — or the local fake, which models cell values only — can detect or verify. Most are documented to auto-adjust on Sheets structural changes; none of that has been independently exercised against a real spreadsheet. A plain formula cell reference (`=Lessons!H5`) auto-adjusts on column insert (guaranteed spreadsheet behavior); an `INDIRECT("Lessons!H5")` string reference does **not** (a genuine, documented Sheets gotcha) and was called out explicitly as an operator pre-check.
- **Locking does not block human edits or the live frontend**, restated explicitly (the README previously implied safety-via-locking without this caveat spelled out as sharply).
- Both are now explicit, dedicated sections in `LESSONS_SCHEMA_MIGRATION_README.md` ("Real Google Sheets behavior — verified vs. unverified", "Locking and concurrency"), plus an expanded, numbered pre-execution checklist and a per-`errorStage` "what actually happened, what to do" table.

**Findings confirmed safe by inspection, no change needed:** the exact full-array-equality design in `lessonsMigrationClassifySchema_` (present since Sprint 6.2A) already safely handles every schema-ambiguity scenario the review tried to construct — trailing blank/nonblank columns, hidden columns (Sheets hiding is a display property only; `getValues()` reads hidden columns normally), duplicated `Type`/`PlacementRule`, extra columns on an otherwise-complete schema, and reordered original headers — because any of these changes the header array's length or content at some position, and the classifier requires exact equality against one of exactly two known-good arrays, never a subset or fuzzy match. This was previously true but not fully tested; Sprint 6.2B added direct test coverage for each (below) rather than changing the (already-correct) logic itself. `Spreadsheet.copy()`'s completeness (formatting, formulas, protections, filters, notes, validations) is guaranteed by documented Apps Script semantics, not something requiring independent verification. `JSON.stringify`-based snapshot comparison (matching the existing `amplifyIm1PlansEqual_`/`cleanupPlansMatch_`/`diagnoseDeepEqual_` pattern already used elsewhere in this codebase) was reviewed for type-coercion/Date/reference-equality risk and found adequate: it deep-serializes (no reference-equality bug class), and none of the 12 original Lessons fields are Date-typed, so the one theoretical Date-serialization edge case does not apply to this specific sheet.

**Tests added — 11 new, all mapped to a specific review finding, none redundant with existing coverage:**
1. An unknown, populated trailing column beyond the audited 12 (neither `Type` nor `PlacementRule` present) is blocked.
2. Both new headers present *and* an extra unknown column (otherwise-complete schema) is blocked.
3. A duplicated `Type` header (with `PlacementRule` absent) is blocked, via the pre-existing duplicate-header path.
4. Both new headers present but appended at the end instead of after `SortOrder` (complete set, wrong order) is blocked.
5. Schema changing between a successful backup and the revalidation pass is caught (`errorStage: "revalidation"`), proving the revalidation pass's real purpose against a simulated concurrent edit landing in exactly that window.
6. Post-write verification detects row reordering with row count and headers otherwise unchanged.
7. Post-write verification detects a value shifted to the wrong semantic header within a row (a simulated column-alignment bug), distinct from a plain content change.
8. Post-write verification detects a nonblank value appearing under a new column.
9. A `flush()` failure after a fully successful mutation still reports `columnsInserted`/`writesOccurred` truthfully (proves correction #1).
10. Rerunning after a failed partial write (columns physically inserted, headers never labeled) is blocked as `unexpected` on the very next attempt — not silently "finished."
11. `verifyLessonsTypePlacementRuleMigration()` never calls `copy()` and never mutates the sheet, even when the schema is already complete (proven by making `copy()` throw if called, and by a before/after byte-identity check).

**Test results.** `node --test scripts/import-staging/lessons-schema-migration.test.mjs` — **29/29 pass** (18 from Sprint 6.2A + 11 new this sprint). Sprint 3's 27 tests and Sprint 4/6.1's 32 tests re-run unchanged and still pass (**88 tests total** across all three suites). `generate-artifact.mjs --check` and `generate-apps-script-payload.mjs --check` both pass with zero drift. `node --check` passes on every modified file; `git diff --check` reports no whitespace errors.

**Not done, and explicitly out of scope for this review:** fixing the same unguarded `lock.releaseLock()` pattern in `ProductionDataCleanup.js`/`AmplifyIm1Importer.js` (noted above, left alone — those files were not part of this review's brief); adding real Apps Script API calls (`getFilter()`, `getProtections()`, `getMergedRanges()`) to surface filter/protection/merged-cell presence in the preview report — considered and deliberately rejected in favor of the narrower documentation-only response, since (a) the code already fails closed via its exception path regardless of which of these is present, and (b) the review's own instructions weighed against broadening the fake spreadsheet's API surface for value that documentation already captures equally well.

---

## 1. Current-State Dependency Map

| Surface | Files | Current assumption | Impact of Type | Impact of flexible placement | Impact of unknown day values | Required change |
|---|---|---|---|---|---|---|
| **Sheets schema** | `Units`, `Lessons` tabs (see `SHEET_STRUCTURE.md`) | Every `Lessons` row is a traditional lesson; `SortOrder` is always a real integer | New optional column needed | `SortOrder` has no "no position" representation today | Blank cell reads back as `""` | Add `Type` column; decide `SortOrder` nullability (§2) |
| **Apps Script reads** | `apps-script-planning/Code.js:435` (`getSheetData`) | Generic — maps header row to keys dynamically; unknown columns pass through untouched | **No change needed** — already forward-compatible | No change needed | Blank cell → `""` in the JSON payload (confirmed: Sheets returns empty string for blank, not null) | None — this function already tolerates new columns |
| **Apps Script writes — `addLesson`** | `Code.js:478` | `PlannedDays: Number(payload.plannedDays \|\| 1)` — blank/omitted defaults to **1**, a real assumed value written to the sheet | Must decide whether to accept/require `type` in the payload; currently omits it entirely from `rowObject`, so new rows get blank Type | N/A (new rows are always fixed-sequence, teacher-authored) | **Existing behavior already writes an assumed default (1), not a blank** — inconsistent with how Forecast reads unconfirmed values (§10) | Add `type` (default `"Lesson"`) to payload/rowObject; reconcile the `\|\| 1` default against §9's "don't invent publisher values" rule (this is teacher-authored data, so inventing a default here is legitimate — the rule applies to *publisher* data, not teacher entry) |
| **Apps Script writes — `updateLesson`** | `Code.js:519` | Same `\|\| 1` default on edit | Would need a `type` update path if type is ever teacher-editable | N/A | Same as above | Minor — add `type` to the updatable fields list if teacher editing of type is desired (not required for import) |
| **Apps Script writes — `deleteLesson`** | `Code.js:559–639` | On delete, renumbers **both** `SortOrder` and `LessonNumber` for every remaining row in the unit to match new physical position | **Breaks `LessonNumber`'s meaning** once non-Lesson rows are interleaved — a Quiz sitting between Lesson 8 and Lesson 9 would cause the real Lesson 9 to be renumbered `LessonNumber = 10` after any delete in that unit | Would also renumber a flexible item into a false fixed position if it has a `SortOrder` at all | N/A | **Must decouple `LessonNumber` (publisher-assigned lesson identity) from `SortOrder` (physical position)** — renumber `SortOrder` for all types, but only renumber `LessonNumber` among rows where `Type === "Lesson"` (or omit renumbering `LessonNumber` for imported rows entirely) |
| **Apps Script writes — `reorderLessons`** | `Code.js:647–736` | Same renumber-both-fields behavior as delete | Same `LessonNumber` conflation risk | Same | N/A | Same fix as `deleteLesson` |
| **Apps Script — no bulk-write/import path exists today** | — | `doPost` recognizes exactly 5 actions; none is a bulk import | N/A | N/A | N/A | A new, separate, manually-invoked function is needed (§7) — there is no existing endpoint to extend safely |
| **Frontend API layer** | `frontend/src/api.js` | `fetchPlannerData()` returns the raw JSON untouched; no normalization of Units/Lessons happens here at all | None — normalization currently happens in each consumer, not centrally | None | Blank/unknown values pass through as `""` unchanged | Consider whether unknown-vs-zero normalization belongs here (once, centrally) instead of duplicated per consumer (§6) |
| **Units** | `frontend/src/components/UnitsView.jsx` | Renders `UnitNumber`, `UnitTitle`, `UnitPurpose`; no Lessons-type awareness at all (lists whatever `Lessons` rows match `UnitID`) | Would need to either group/label items by type or continue rendering them undifferentiated | Would need to exclude or separately present flexible items | No current UI signal for "this unit's day budget is unconfirmed" | Add type-aware display (§9) |
| **Forecast** | `frontend/src/utils/forecastModel.js`, `frontend/src/utils/plannerUtils.js` | Sums `Number(unit.RequiredDays \|\| 0)`, `Number(unit.OptionalDays \|\| 0)` (`plannerUtils.js:64-76`) and `Number(lesson.PlannedDays \|\| 0)` (`forecastModel.js:36`) unconditionally across **every** row for a course; walks lessons via `sortLessons` (`plannerUtils.js:82-95`) using `Number(a.SortOrder)` | Currently has no way to exclude non-Lesson rows from "current lesson" selection or day-cost sums | `sortLessons` uses `Number(a.SortOrder) - Number(b.SortOrder)`; `Number(undefined)` is `NaN`, and `Array.prototype.sort` comparator behavior with `NaN` is **unstable/undefined** — a flexible item with no `SortOrder` does not sort predictably today, it doesn't fail loudly | **This is the silent-zero risk described in `CURRICULUM_INFORMATION_MODEL.md` §10, confirmed at the exact line level** — an unconfirmed `RequiredDays`/`PlannedDays` is indistinguishable from a real zero | Must not call `sortLessons` on flexible-placement rows at all (filter first); must distinguish unknown from zero (§6) |
| **Planning** | `frontend/src/utils/planningModel.js` | Filters `Lessons` by `UnitID`, sorts via `sortLessons`, presents next N as the "shelf" (`planningModel.js:176-184`); no type filtering exists because no type exists | Shelf currently shows every row for a unit undifferentiated | Same `sortLessons` fragility as Forecast | N/A (Planning doesn't sum days) | Filter shelf to sequenced items only; separately surface flexible items (§9) |
| **Lesson Session linkage** | `frontend/src/utils/lessonSessionStorage.js`, referenced via `curriculumLessonId` in `planningModel.js:144-149` | A session may optionally store `curriculumLessonId` pointing at any `Lessons.LessonID`; the link is read-only citation, never structural | **No change required** — the link is already type-agnostic; it resolves by ID regardless of what the row's Type is | No change required — a session could already reference a flexible item's ID the same way it references any other | No change required | None — this surface is already forward-compatible by design |
| **Print** | `frontend/src/utils/lessonPrintPayload.js` | `getEpisodeLesson()` looks up `curriculumLessons.find(l => l.LessonID === lessonId)` (`lessonPrintPayload.js:5-12`) — type-agnostic lookup | No change required for basic function; could optionally show item type as a label | No change required | No change required | None required; optional label enhancement only (§9) |
| **Weekly Communication** | `frontend/src/utils/weeklyCommunication.js` | Reuses `buildLessonPrintPayload`; same type-agnostic lookup | Same as Print | Same as Print | Same as Print | None required |
| **DailyProgress** | `Code.js:458` (`saveDailyProgress`), `SHEET_STRUCTURE.md` | References `LessonID` directly; assumes the referenced row is a lesson whose completion should count toward pacing | Logging progress against a non-Lesson `LessonID` (e.g. a Quiz) is already possible today and already silently counts in Forecast's sums — nothing currently prevents it | N/A | N/A | Decide whether logging progress against non-Lesson types should be allowed, encouraged, or filtered (§5) |
| **Seed data** | — | **No Units/Lessons seed function exists in `Code.js`.** The only seed function present (`setupRosterSheetsV1`, `Code.js:139-374`) is for roster tables, not curriculum. | N/A | N/A | N/A | The importer is genuinely new — there is no existing curriculum-seeding code to extend, only a *pattern* to follow (§7) |
| **Tests/validation** | — | **No automated test suite for Units/Lessons was found.** The closest precedent is `apps-script-roster-admin/ProductionDataAudit.js` and `ProductionDataCleanup.js` — a read-only audit function and a preview/execute cleanup pair, both `LockService`-guarded | N/A | N/A | N/A | Recommend the importer follow this exact precedent rather than inventing a new pattern (§7) |
| **Documentation** | `SHEET_STRUCTURE.md`, `API_REFERENCE.md`, `UNITS_ARCHITECTURE.md`, `INFORMATION_MODEL.md` | Describe `Lessons` as lesson-only records; `SHEET_STRUCTURE.md` was already corrected once this sprint (`Optional` → `IsOptional`, see the Curriculum Information Model Review) | Will need a field addition once `Type` is implemented (not yet — see Non-Decisions) | N/A | N/A | Update only when the schema change actually ships, not now |

---

## 2. Proposed Schema Evolution

Minimum evolution — no field is added without a stated purpose, owner, and consumer.

| Field | Purpose | Owner | Required/Optional | Example values | Backward compatibility | Consumers | Migration impact |
|---|---|---|---|---|---|---|---|
| **`Type`** *(name below is a recommendation, not this spec's decision — see `CURRICULUM_INFORMATION_MODEL.md` §5)* | Preserve the publisher's literal instructional-item type | Publisher (import-time), never teacher-edited in v1 | Logically required for every row going forward; **physically optional** — blank is valid and means `Lesson` | `Lesson`, `Explore`, `Practice Day`, `Sub-Unit Quiz`, `Performance Task`, `Investigate`, `Meet & Greet` (verbatim strings from the extraction) | Blank/missing = `Lesson`. Every existing row today has no `Type` value and is therefore, correctly, a `Lesson` under this rule with zero migration required. | Planning shelf filtering, Forecast sequence walk, Units display, Print (optional label) | **None required** — additive column, default behavior preserves every existing row's meaning exactly |
| **`SortOrder`** | Physical position within a unit | Mixed: publisher-sequenced for imported rows, system-assigned for teacher-added rows | Required for fixed-sequence items; **must become nullable** for flexible-placement items | `1, 2, 3…`; blank for a flexible item | Existing rows keep their existing values unchanged | Forecast, Planning, `sortLessons()` | `sortLessons()` and any other consumer must filter out rows with no `SortOrder` **before** sorting, not rely on the sort to skip them safely (confirmed unsafe today — §1) |
| **`PlacementRule`** *(new, optional)* | Store the publisher's own flexible-placement text verbatim, for items with no fixed `SortOrder` | Publisher | Optional; only populated for flexible items | `"anytime in this course after Unit 5, Lesson 15"` | Absent on every existing row; absent = "not a flexible item," consistent with `SortOrder` being present instead | Units display (to explain *why* an item has no position), importer validation | None — new rows only |
| **`LessonNumber`** *(existing field, semantics clarified, not renamed)* | The publisher's own "Lesson N" label — an identity, not a position | Publisher | Required for `Type = "Lesson"` rows; **meaningless for other types and should not be invented for them** | `1, 2, 3…` (restarts within each sub-unit's lesson count, per the extraction) | Existing rows unaffected | Print/UI labels ("Lesson 3: …") | Apps Script's delete/reorder renumbering logic must stop treating this as derivable from physical position (§1, §9) — this is a **behavior fix**, not a schema fix |
| **`IsOptional`** *(existing field, no change)* | Item-level skippability | Publisher | Optional, defaults to falsy | `true`/`false` | Already present in production; already populated by the extraction | Not currently consumed anywhere (§1) — activating consumption is Recommended, not Essential | None |
| **`SourceProvenance`** *(new, optional — recommended, not essential)* | Record where an imported row came from, for safe re-import and audit | System (import-time) | Optional | `"amplify-im1-u3-import-2026-08"` | Absent on every existing row | Importer idempotency/duplicate-detection (§7) | None — additive |
| **`PlannedDays`** *(existing field, no schema change, behavior clarified)* | Per-item day cost | **Teacher**, never publisher (per `CURRICULUM_INFORMATION_MODEL.md` §9) | Optional (unknown ≠ zero, §6) | A number, or blank meaning "not yet estimated" | Unchanged | Forecast | None to the schema; **the risk is entirely in how blank is read downstream (§6, §10)** |

**IDs and naming:** existing `UnitID`/`LessonID`/`CourseID` conventions (`IM1-U3`, `IM1-U3-L5`) can be preserved unchanged. Non-Lesson Instructional Items can use the same `{UnitID}-L{n}` pattern (an ID prefix does not need to encode Type) or a distinct prefix (`{UnitID}-I{n}`) — this spec recommends **keeping the existing `-L{n}` pattern** for all Instructional Item types, since `LessonID` already means "row identifier in the Lessons/Instructional-Items table," not "identifier of a traditional lesson specifically," and changing it would touch every existing consumer's ID-matching logic for no functional benefit.

---

## 3. Literal Type Strategy

| Literal value (exact string) | Store verbatim? | Conceptual role (for logic, not storage) | Notes |
|---|---|---|---|
| `Meet & Greet` | Yes | Orientation | No publisher type prefix in source at all; stored as its own literal per `IM1_Curriculum_Extraction.md` treatment |
| `Pre-Unit Check` | Yes | Diagnostic | Always optional, always first or second position |
| `Explore` | Yes | Launch/hook | Always optional |
| `Lesson` | Yes | Instructional | The default and the majority case |
| `Practice` | Yes | Practice | Literal term used in Units 3–5 (pre-methodology-shift extraction) |
| `Practice Day` | Yes | Practice | Literal term used in Units 1, 2, 6, 7 |
| `Mid-Unit Check` | Yes | Assessment (formative) | Literal term used in Units 3–5, teacher-confirmed mapping from "Sub-Unit Quiz" |
| `Sub-Unit Quiz` | Yes | Assessment (formative) | Literal term used in Units 2, 6, 7 — same conceptual role as `Mid-Unit Check` |
| `Assessment` | Yes | Assessment (summative) | Literal term used in Units 3–5 for the Performance Task |
| `Performance Task` | Yes | Assessment (summative) | Literal term used in Units 1, 2, 6, 7 — same conceptual role as `Assessment` |
| `Reflection` | Yes | Reflective | Literal term used in Units 3–5 |
| `Unit Synthesis and Reflection` | Yes | Reflective | Literal term used in Units 1, 2, 6, 7 — same conceptual role as `Reflection`; Title is this exact phrase in **every** unit regardless of which Type vocabulary that unit uses |
| `Investigate` | Yes | Flexible-placement, optional | Never has a `SortOrder`; always carries a `PlacementRule` |

**Storage decision:** literal values are stored exactly as found — no normalization at import time. **A separate conceptual-role mapping is not needed in the schema right now.** Where code needs to reason about role rather than literal string (e.g. "is this the unit's summative assessment, regardless of which unit's vocabulary it uses"), that mapping belongs in **code or configuration**, not in a stored field — a small lookup table (`{"Assessment": "summative", "Performance Task": "summative", ...}`) living in the frontend or importer, easily extended when a new literal appears. This keeps the source-of-truth data untouched by a mapping decision that may itself evolve.

**Unknown-type behavior:** any consumer encountering a `Type` value not in its own lookup must treat it as an ordinary `Lesson` for sequencing/day-math purposes (safe degrade, per `CURRICULUM_INFORMATION_MODEL.md` §5) while still displaying the literal value verbatim wherever type is shown. It must never crash, drop the row, or silently discard it.

---

## 4. Ordering and Flexible Placement

**Test cases:** `Investigate: Tuition Costs` (Unit 5) and `Investigate: Exploring Climate Change` (Unit 7) — both have no `SortOrder` in the extraction and both carry the identical placement text pattern ("anytime in this course after Unit N, Lesson [last lesson]").

Consumer-by-consumer behavior:

| Consumer | Behavior with a flexible item |
|---|---|
| **Forecast current-item selection** | Must exclude flexible items from the sequential walk entirely — they never become "the current lesson" and never contribute to `plannedDaysCompleted` sums by position. If a teacher does log progress against one (via `DailyProgress`), that's a valid `IsOptional`/teacher decision (§5), separate from sequence walking. |
| **Planning shelf** | Must not appear in the ordered shelf. Presented separately (e.g., a small "anytime this unit" list) or not surfaced in Planning at all in v1 — this spec does not prescribe which; either satisfies the "no fabricated position" rule. |
| **Units display** | May be shown, using the stored `PlacementRule` text (§2) to explain *why* it has no position, rather than silently omitting it or wedging it into the sequence. |
| **Importer validation** | Must recognize "no `SortOrder` present" as a valid, expected state for these two items specifically — not an incomplete-import error. |
| **Print and Weekly Communication** | Already type-agnostic (§1) — no change needed; if a teacher links a session to a flexible item, it prints exactly like any other citation. |
| **Future editing** | If a teacher ever needs to manually schedule a flexible item onto a specific day, that's expressed as a `DailyProgress` entry or a Lesson Session link on that date — **not** by retroactively giving the `Lessons` row a fabricated `SortOrder`. The row's identity stays flexible; only the teacher's choice to teach it on a given day is recorded, elsewhere. |

**The one rule that governs all of the above:** no code path may assign a `SortOrder` to a row whose source explicitly has none. Absence must remain absence, all the way through the system.

---

## 5. Optionality Behavior

| Question | Technical requirement | Product decision needed? |
|---|---|---|
| Do optional items appear in Planning by default? | Technically trivial either way (a filter). | **Yes** — recommend: appear, but visually de-emphasized, so teachers see the full unit but aren't pressured to use every item. |
| How are they visually distinguished? | Requires a UI treatment. | **Yes** — recommend a small label/badge, not a structural difference, to avoid a larger design task. |
| Do they count toward Forecast progress? | Technically: only if `DailyProgress` references them, same as any row today. | No product decision required — this falls out of existing behavior; optional items already only affect Forecast if the teacher logs progress against them. |
| Does skipping one require an explicit completion state? | Not currently — there's no "skipped" state in `DailyProgress` today (only `Finished` boolean). | **Yes, but not blocking** — recommend deferring; today's model (simply never log it) already expresses "skipped" adequately for v1. |
| Do optional items contribute to `PlannedDays`? | Only if a value is entered, same as any row. | No — falls out of §6 either way. |
| Does optionality affect current-item selection? | **Yes, this needs a rule.** If Forecast's "current lesson" walk includes an unstarted optional item, a teacher who deliberately skips it would appear stuck. | **Yes** — recommend: Forecast's sequence walk skips over an optional item once it's either completed **or** explicitly bypassed by moving on to the next required item; default behavior for v1 should not block progression on an optional item. |
| How do optional flexible items behave? (e.g. `Investigate`) | Both properties apply independently — already excluded from sequence (§4) and already never blocking. | No additional decision — the two rules compose without conflict. |

The one item requiring product input before Planning/Forecast are updated is flagged as **Decision D-1** in §14.

---

## 6. Planning-Day Strategy

**Current confirmed state:** Unit day budgets (`RequiredDays`/`OptionalDays`) are confirmed for **1 of 7** units (Unit 3: 17/3). Per-item `PlannedDays` is confirmed **absent from the publisher source for all 7 units** — this is not a gap to close by re-reading the PDFs harder; Amplify does not publish it at this granularity.

- **Storing unknown values:** a genuinely unknown day count is stored as a **blank cell**, not a zero and not a placeholder string. This matches current Sheets behavior exactly — no schema change needed, only a change in how blank is *interpreted* downstream.
- **API representation of unknown vs. zero:** `getSheetData()` already returns `""` for a blank cell (§1) — this is already distinguishable from a real `0` if consumers check for it. The fix is entirely in the frontend's `Number(x || 0)` pattern, which currently collapses both cases to the same value. Recommend a small shared helper (e.g. `parseKnownNumber(value)` returning `null` for blank/unparseable input, a real number otherwise) used everywhere `Number(x || 0)` appears today, so "unknown" and "zero" become distinguishable at every call site without touching the sheet.
- **Frontend display of unknown values:** show a distinct state (e.g. "Not yet planned" or "—") rather than "0 days," anywhere a day count is rendered.
- **Forecast behavior when values are unknown:** recommend an explicit **incomplete-data state** for any unit missing a confirmed day budget, or any section whose current unit's lessons are missing `PlannedDays` — Forecast should say "pacing not available for this unit yet," not silently compute a number that happens to be wrong. This is the direct fix for the silent-zero risk (`CURRICULUM_INFORMATION_MODEL.md` §10).
- **Where teacher entry/approval occurs:** the natural point is Units (where `RequiredDays`/`OptionalDays` already live conceptually) for unit-level budgets, and either Units or Planning for per-item `PlannedDays`. This spec does not prescribe the exact UI — only that entry happens through an existing teacher-facing surface, not a one-time import script guess.
- **Initialization assistant or allocation tool:** **candidate options only, not a decision:**
  - *Option A — manual entry, no tooling.* Teacher fills in each unit's day budget and each item's planned days directly, at their own pace. Simplest to build (nothing to build); slowest for the teacher.
  - *Option B — even-split default, teacher-adjustable.* System proposes `PlannedDays = RequiredDays / (count of Lesson-type items)` per unit, clearly labeled as a system estimate, editable before being treated as real. Faster for the teacher; carries a real risk of the estimate being silently trusted past the point it should be reviewed.
  - *Option C — deferred entirely.* Import curriculum content now; leave all day-budget fields blank; treat Forecast as intentionally unavailable for IM1 until a separate, later planning pass fills them in.
  
  This spec recommends **Option C for the initial IM1 import**, with Option B as a plausible fast-follow once a product decision confirms it — because Option B's "system estimate" must never be mistaken for publisher truth (§8 of the governing ADR), and that labeling discipline is easier to get right in a dedicated, later pass than bundled into the first import.
- **Validation before Forecast is trustworthy:** every unit in the course must have a confirmed `RequiredDays`/`OptionalDays`, and every `Lesson`-type item a teacher expects Forecast to pace against must have a `PlannedDays` value — both **teacher-confirmed**, not system-inferred and left unreviewed.

---

## 7. Importer Design

**Precedent this design follows:** `apps-script-roster-admin/ProductionDataAudit.js` (read-only audit) and `ProductionDataCleanup.js` (`previewProductionDataCleanupV1` / `executeProductionDataCleanupV1`, `LockService`-guarded, backup-before-write, plan-object shared between preview and execute). No comparable precedent exists for curriculum data — this would be new code, following an established pattern rather than inventing one.

| Aspect | Specification |
|---|---|
| **Input format** | `IM1_Curriculum_Extraction.md`'s per-unit tables, treated as the single source of truth for what to import |
| **Source document** | The extraction file itself — not a re-read of the original PDFs at import time; the extraction is the already-verified intermediate |
| **Parsing/transformation approach** | A one-time, human-supervised transcription from the extraction's Markdown tables into a machine-readable intermediate (see "staged approach" below) — not an automated Markdown parser, given the extraction's own inconsistencies (two Type vocabularies, per-unit anomalies) are more safely handled by a human re-reading them once than by parsing logic that would need to special-case each one |
| **Validation stages** | (1) structural — every row has `UnitID`, `Title`, `Type`; (2) referential — every `UnitID` referenced exists; (3) semantic — every fixed item has a `SortOrder`, every flexible item does not; (4) content — item counts and titles match the extraction exactly, spot-checked against `IM1_Curriculum_Extraction.md` |
| **ID generation** | Follow the existing `{UnitID}-L{n}` convention (§2); `n` assigned by import-time position within the unit, not by the publisher's own numbering (which restarts per sub-unit and isn't unique) |
| **Idempotency** | Re-running the importer against the same source must not create duplicate rows — checked via `SourceProvenance` (§2) or, absent that field, via an exact-match check on `UnitID` + `Title` + `Type` |
| **Duplicate detection** | Same mechanism as idempotency; surfaced in the preview report as "already present, will be skipped" |
| **Update behavior** | **Never silently overwrite a row that has any teacher-authored field populated** (`TeacherNotes`, a non-default `PlannedDays`, or any evidence of being referenced by a `DailyProgress`/Lesson-Session link). An update to publisher-owned fields (Title, Type, Summary) on an already-imported row is safe; an update that would touch `PlannedDays` or `TeacherNotes` must be skipped and flagged, never applied automatically |
| **Dry-run behavior** | Mandatory first step. Produces the exact same "plan" object the execute step would use, with zero writes |
| **Preview/reporting** | Human-readable summary (rows to add, rows to skip, rows flagged for manual review) plus a structured JSON report, following `ProductionDataCleanup.js`'s existing dual-format precedent exactly |
| **Error handling** | Fail before any write on any structural/referential validation failure — no partial import |
| **Rollback strategy** | Follow `ProductionDataCleanup.js`'s pattern: create an explicit backup (`cleanupCreateBackup_`) immediately before the first write, and provide a documented manual restore path from that backup. Apps Script has no cross-sheet transaction (noted directly in `Code.js:313-314`'s comment for the roster seeder) — rollback is "restore from backup," not "undo," and must be stated as such up front |
| **Separation of source-derived vs. teacher-authored fields** | Enforced structurally: the importer only ever writes to fields classified as Publisher-owned in `CURRICULUM_INFORMATION_MODEL.md` §8. It never writes `PlannedDays`, `TeacherNotes`, or `PrimaryLink` with anything other than blank |
| **Treatment of extraction notes/anomalies** | Each of the extraction's 18 Extraction Notes is either (a) already resolved and requires no special importer handling (e.g. Unit 3's day totals), or (b) requires a specific, named handling rule at import time — enumerated below |
| **Treatment of flexible items** | Imported with `Type = "Investigate"`, `PlacementRule` populated from the source text, `SortOrder` left blank — never assigned a position |
| **Treatment of unknown day values** | Imported as blank, per §6 — never defaulted to a guessed number by the importer itself |

**Named anomaly handling** (from the extraction's own notes):
- Unit 4's triplicated "Practice Day 2" source pages → import once, as already deduplicated in the extraction.
- Unit 2's stray embedded asset URL in the Practice Day 1 summary → already excluded from the extraction's `Summary` field; importer uses the extraction's cleaned text, not the raw source.
- "Meet & Greet" (Unit 1) → imported with `Type = "Meet & Greet"` exactly, `SortOrder = 1` (it does have a fixed position, unlike Investigate) — no special-case needed beyond accepting an unrecognized-but-valid literal type.
- Unit 1's missing quiz → no quiz row imported for Unit 1; validation must not treat "zero quiz rows in this unit" as an error.

**Staged approach recommendation:** **produce an intermediate machine-readable artifact first** (e.g. a reviewable JSON or CSV generated from the extraction, checked in or attached to the sprint, not yet written to the spreadsheet), rather than writing directly through Apps Script or generating spreadsheet rows blind. Reasoning: the extraction itself has already gone through several rounds of correction (Units 1 and 2 were fully re-extracted after being found wrong); an intermediate artifact gives one more inspectable checkpoint between "verified against the PDF" and "written to production" without adding meaningful delay, and it's the artifact the dry-run/preview step (above) would naturally consume. **This is the safest initial path given the current repository's precedent** — it mirrors `ProductionDataCleanup.js`'s plan-object approach one layer earlier, before any spreadsheet is touched at all.

---

## 8. Migration Strategy

| Item | Migration treatment |
|---|---|
| Existing `Lessons` rows without `Type` | No migration required — blank means `Lesson`, which is already true of every existing row (§2) |
| Existing `SortOrder` | Unchanged; no existing row needs its value touched |
| Existing `LessonNumber` | Unchanged in value; **behavior** of `deleteLesson`/`reorderLessons` needs a fix (§1, §9) so future operations don't corrupt it once non-Lesson rows exist — this is a code change, not a data migration |
| Existing `PlannedDays` | Unchanged — Math 8's existing lessons keep whatever values they already have |
| Existing `IsOptional` | Unchanged |
| Existing `DailyProgress` references | Unaffected — they reference `LessonID`, which doesn't change for existing rows |
| Existing Lesson Session `curriculumLessonId` references | Unaffected — the link resolves by ID regardless of Type (§1); nothing to migrate |
| Seed/production data (Math 8) | Not touched by this work at all — Math 8's curriculum is untouched; only IM1 rows are added |
| `localStorage` sessions | Unaffected — Lesson Session storage doesn't inspect `Lessons` schema shape, only the ID it was given at link time |
| **Read compatibility during phased deployment** | Safe at every phase: `getSheetData()` is already forward-compatible (§1); a frontend build that doesn't yet know about `Type` simply ignores the new column, exactly as it ignores any unknown key today |
| **Write compatibility during phased deployment** | Safe as long as `addLesson`/`updateLesson` are updated to include a `type` field **before** any Instructional-Item-aware frontend ships that assumes it — sequencing this correctly is Implementation Sequence step 3 (§10) |

**No step in this migration requires downtime or a frontend/backend version to move in lockstep** — every change here is additive-first, behavior-fix-second.

---

## 9. Consumer-by-Consumer Changes

#### Apps Script
- **Schema reads:** none — `getSheetData()` already works (§1).
- **Schema writes:** `addLesson`/`updateLesson` (`Code.js:478`, `:519`) — add `type` to `rowObject`/`updates`, defaulting to `"Lesson"`.
- **Validation:** `deleteLesson`/`reorderLessons` (`Code.js:559-736`) — stop unconditionally renumbering `LessonNumber`; scope that renumbering to `Type === "Lesson"` rows only (or drop it for imported rows entirely, leaving `LessonNumber` as a publisher-assigned constant once set at import).
- **Compatibility defaults:** none beyond the above — `getSheetData()` needs no change.
- **Import endpoint/script:** a new, separate, manually-invoked function (not wired to `doPost`), following `ProductionDataAudit.js`/`ProductionDataCleanup.js`'s exact shape — preview function, execute function, `LockService`, explicit confirmation string, backup-before-write.

#### Frontend API layer
- **Normalization:** introduce the shared "known vs. unknown number" helper described in §6, used at every current `Number(x || 0)` call site.
- **Unknown numeric handling:** blank stays distinguishable from zero through to the component layer.
- **Type defaults:** any row read without a `Type` value is treated as `"Lesson"` — likely a small helper (`getItemType(lesson)`) rather than scattering `lesson.Type || "Lesson"` everywhere.
- **Flexible-placement representation:** a row with no `SortOrder` is recognized as flexible by every consumer that currently calls `sortLessons` — filter before sorting, per §4.

#### Units
- **Display changes:** group or label items by type once `Type` exists; show `PlacementRule` text for flexible items instead of silently omitting them.
- **Day-budget status:** show an explicit "day budget not yet confirmed" state for any unit missing `RequiredDays`/`OptionalDays`, rather than showing a blank or a zero.

#### Forecast
- **Type-aware progression:** the sequential walk (`forecastModel.js`) excludes flexible items and, per the product decision in §5/§14, may skip over unstarted optional items so they don't block "current lesson."
- **Optional-item behavior:** per §5's recommendation, optional items don't block sequence progression by default.
- **Unknown-value state:** an explicit incomplete-data state per unit/section, replacing today's silent `0`.
- **Prevention of silent-zero math:** every `Number(x || 0)` in `forecastModel.js`/`plannerUtils.js` (specifically `plannerUtils.js:64-76` and `forecastModel.js:29-36`) is replaced with the known/unknown-aware helper (§6, §9).

#### Planning
- **Fixed-sequence shelf:** unchanged in mechanism, filtered to exclude flexible items.
- **Optional items:** shown with the visual de-emphasis recommended in §5, pending the product decision.
- **Flexible-placement items:** presented separately if presented at all in v1 (§4) — this spec does not mandate a specific mechanism.
- **Unknown types:** degrade to ordinary-Lesson shelf treatment (§3).

#### Lesson Session
- **Curriculum linking:** unchanged — `curriculumLessonId` already resolves by ID regardless of Type (§1).
- **Backward compatibility:** full — nothing about Lesson Session storage or its schema needs to change for this work.
- **Print/citation effect:** none — already type-agnostic.

#### Print and Weekly Communication
- **Should item type appear?** Optional enhancement only — e.g. showing "Practice Day" instead of a bare lesson-style citation, if a session links to one. Not required for v1.
- **Safe fallback:** an unlabeled or generically-labeled citation if type information is ever missing — never a broken print.
- **No-break expectation:** confirmed safe today (§1) and remains safe after every change proposed above, since none of them touch `lessonPrintPayload.js`'s actual lookup logic.

---

## 10. Implementation Sequence

Each step is independently verifiable and, except where noted, touches no production data.

| # | Objective | Files likely affected | Prerequisites | Verification | Rollback point | Touches production data? |
|---|---|---|---|---|---|---|
| 1 | Documentation and schema contract | `SHEET_STRUCTURE.md`, this spec, the governing ADR (already done) | None | Docs reviewed | Revert doc edit | No |
| 2 | Backward-compatible read support (known/unknown numeric helper; `Type` default-to-`Lesson` helper) | `plannerUtils.js`, new small util | Step 1 | Unit-level manual check: existing Math 8/IM1 data renders identically to before | Revert commit | No |
| 3 | `Type` field addition (Apps Script write paths + sheet column) | `Code.js` (`addLesson`, `updateLesson`), `Lessons` sheet header | Step 2 | Add a test lesson via the UI; confirm it gets `Type = "Lesson"` and nothing else changes | Remove column, revert `Code.js` | **Yes — additive column only, no existing data touched** |
| 4 | Unknown numeric-value handling in Forecast | `forecastModel.js`, `plannerUtils.js` | Step 2 | Forecast still renders correctly for Math 8 (fully-populated data); a synthetic unit with blank days shows the new incomplete-data state instead of a silent zero | Revert commit | No |
| 5 | Flexible-placement support (filtering before `sortLessons`) | `plannerUtils.js`, `planningModel.js`, `forecastModel.js` | Steps 3, 4 | Synthetic row with no `SortOrder` is excluded from Planning shelf and Forecast's sequence walk, doesn't crash either | Revert commit | No |
| 6 | Consumer updates (Units display, Print label, `deleteLesson`/`reorderLessons` `LessonNumber` fix) | `UnitsView.jsx`, `Code.js`, `lessonPrintPayload.js` (optional) | Steps 3–5 | Manual click-through of Units page; delete a lesson in a unit with a mixed Type set (once test data exists) and confirm `LessonNumber` isn't corrupted for `Lesson`-type rows | Revert commit | No |
| 7 | Importer dry run (against the extraction, producing the intermediate artifact from §7) | New script/artifact, not yet in `apps-script-planning` | Steps 3–6 shipped | Preview report reviewed by teacher; row counts match the extraction exactly (§11 Data validation) | Discard artifact | No |
| 8 | Test import into non-production data (a scratch spreadsheet or a clearly-marked test tab) | Import script execution | Step 7 approved | Application-level validation (§11) against the test data | Delete test data | **No — explicitly not production** |
| 9 | Teacher planning-data completion (Required/Optional Days confirmed for all 7 units; `PlannedDays` decision applied per §6) | Production spreadsheet, teacher-entered | Independent of 1–8; can happen in parallel | Every unit has a confirmed day budget or an explicit, reviewed decision to leave it blank | N/A (data entry) | **Yes, but additive/corrective only, teacher-directed** |
| 10 | Production import | Production `Units`/`Lessons` sheets | Steps 7–9 complete; backup created per §7 | Post-write readback matches the dry-run plan exactly | Restore from backup (§7) | **Yes** |
| 11 | Forecast validation | None (verification step) | Step 10 | Forecast page reviewed against real IM1 data; incomplete-data states appear only where genuinely expected | N/A | No |
| 12 | Classroom validation | None (verification step) | Step 11 | A full planning cycle exercised against real IM1 content, per `CLASSROOM_READINESS.md` Section E | N/A | No |

---

## 11. Validation Plan

**Static**
- Every `Lessons` row has a non-blank `UnitID`, `Title`.
- `Type` inventory contains only recognized literals or is explicitly flagged for review if a new one appears.
- No duplicate `LessonID` values.
- No row has both a `SortOrder` and a `PlacementRule` (fixed and flexible are mutually exclusive).

**Data**
- All 7 units present, matching `IM1_Curriculum_Extraction.md` unit-by-unit.
- Item counts per unit match the extraction exactly (e.g. Unit 6 = 30 fixed-sequence items).
- Titles and literal Types match verbatim.
- `IsOptional` flags match the extraction's `Optional Item?` column.
- Both flexible items (Unit 5, Unit 7 `Investigate`) present with no `SortOrder` and a populated `PlacementRule`.
- No existing teacher-authored field (`TeacherNotes`, non-blank `PlannedDays` on a pre-existing row) is altered.

**Application**
- Units renders all 7 IM1 units without error.
- Planning shelf shows fixed-sequence items in order; excludes or separately presents flexible items; doesn't crash on an unrecognized Type.
- Forecast handles both a fully-confirmed unit (Unit 3) and an incomplete one (any other IM1 unit) without silently computing a wrong number.
- Existing Lesson Session links (Math 8, any pre-existing IM1 test data) still resolve after the schema change.
- Print still produces correct output for both linked and unlinked sessions.
- Weekly Communication still produces correct output.

**Production**
- Read-only audit of the production sheet immediately before any write (following `auditProductionDataV1()`'s pattern).
- Dry run produces a plan with exact expected row counts, reviewed by the teacher before execution.
- Guarded execution: explicit confirmation string required, `LockService`-protected, backup created first.
- Post-write readback compares the sheet's actual new state against the dry-run plan, row for row.
- Documented restore-from-backup procedure, exercised (at least once, in the test-import step) before it's ever relied on in production.

---

## 12. Test Matrix

| Case | Expected behavior |
|---|---|
| Legacy Lesson row with blank Type | Treated as `Lesson`; renders and paces identically to today |
| Ordinary fixed Lesson | Sequenced normally in Planning and Forecast |
| Optional Explore | Appears (de-emphasized per §5); doesn't block sequence progression once past |
| Practice Day | Sequenced normally; counted in day-math once `PlannedDays` is known |
| Sub-Unit Quiz | Sequenced normally; recognized as an assessment role in code where relevant, stored literally in data |
| Performance Task | Sequenced normally; last-but-one position preserved |
| Unit Synthesis and Reflection | Sequenced normally; last position preserved |
| Unit 1 with no quiz | No error from "zero quiz rows" anywhere in validation or UI |
| Unit 1 Meet & Greet | Renders with its literal, unrecognized-elsewhere Type; positioned first |
| Unit 5 Investigate | No `SortOrder`; excluded from Forecast's sequence walk and (per §4) from the ordered Planning shelf; doesn't crash `sortLessons` |
| Unit 7 Investigate | Same as above, confirming the pattern holds for a second instance |
| Unknown future Type | Degrades to ordinary-Lesson treatment everywhere; literal value still displayed if type is shown at all |
| Unit with unknown RequiredDays | Forecast shows an explicit incomplete-data state, not a silent zero |
| Item with unknown PlannedDays | Excluded from variance math (or flagged), never silently treated as zero cost |
| Completed item | Behaves exactly as today — `DailyProgress` reference, counted normally |
| Skipped optional item | Simply never logged in `DailyProgress`; no error, no forced state |
| Existing Lesson Session link to a legacy row | Resolves exactly as before — confirms §1's "no change required" claim for Lesson Session linkage |

---

## 13. Risks and Rollback

| Risk | Likelihood | Impact | Mitigation | Rollback |
|---|---|---|---|---|
| Silent zero coercion (unconfirmed day values treated as real zeros) | **High if not addressed** — it's today's default behavior, confirmed at the code level (§1, §10) | High — corrupts whole-course Forecast math, not just the affected unit | Known/unknown-aware helper (§6, §9), applied before any IM1 unit is turned on in Forecast | Revert the helper change; no data-level rollback needed since nothing was written incorrectly, only displayed incorrectly |
| Accidental overwrite of teacher-authored data on re-import | Medium — only if the importer's update path isn't built carefully | High — could destroy real planning work, the system's highest-priority protection per `PROJECT_CONTEXT.md` | Importer never writes non-blank teacher-owned fields (§7); idempotency check before any write | Restore from pre-import backup |
| Duplicate import | Medium without idempotency check | Medium — clutter, confusing Forecast counts | `SourceProvenance` or exact-match duplicate detection (§7) | Restore from backup, or a targeted delete of the duplicate rows via existing `deleteLesson` |
| Breaking current lesson ordering (Math 8, existing IM1 seed data) | Low | High if it happened — affects daily classroom use immediately | Every schema/behavior change in this spec is additive-first (§8); no existing row's values are altered | Revert the specific code change |
| Breaking existing IDs or `DailyProgress` references | Low | High | ID convention preserved exactly (§2); no existing `LessonID` is renamed or reassigned | Revert |
| Treating flexible items as fixed | Medium if `sortLessons` isn't updated carefully | Medium — a flexible item would silently gain a false position | Explicit filter-before-sort at every call site (§4, §9), tested directly (§12) | Revert the specific consumer change |
| Consumer assumption "every row is a Lesson" (in code not yet updated) | Medium — this is the default state of every consumer today | Low individually (most consumers are already type-agnostic, per §1), but real for `sortLessons`/Forecast specifically | Type-default-to-Lesson (§3) makes this safe by construction for any consumer not yet updated | Revert |
| Phased-deployment incompatibility (frontend/backend version mismatch) | Low | Low — confirmed safe both directions in §8 | Additive-only sequencing (§10) | Revert whichever side shipped out of order |
| Documentation drifting from implementation | Medium over time | Low immediately, compounding later | This spec and the governing ADR both explicitly separate "implemented" from "proposed" — keep that discipline when steps actually ship | Update docs alongside the shipping code, not after |

---

## 14. Decisions Required Before Coding

**All five decisions below are approved.** Table retained in its original question/options/recommendation form for traceability; the "Approved" column records the final answer actually adopted.

| ID | Question | Options | Recommendation | Approved | Impact of delaying | Can coding start first? |
|---|---|---|---|---|---|---|
| **D-1** | Should an unstarted optional item block Forecast's "current lesson" progression? | (a) Yes, must complete before advancing; (b) No, skip over it automatically once the next required item is reached | (b) — matches how a teacher actually plans around optional content | **(b) approved as recommended.** Optional instructional items do not block Forecast progression. | Steps 4–5 (§10) can be built either way, but the exact skip rule needs this answer before shipping | **Yes** — steps 1–4 don't depend on this answer |
| **D-2** | Where does teacher planning-day entry occur — Units, Planning, or a dedicated screen? | (a) Units; (b) Planning; (c) new dedicated surface | (a) Units, since `RequiredDays`/`OptionalDays` already conceptually live there | **(a) approved as recommended.** Teacher planning-day entry will live in the Units workspace. | Blocks step 9 (§10) only — not earlier steps | **Yes** |
| **D-3** | Should item Type labels appear in the teacher-facing UI at all in v1, or stay internal? | (a) Show labels (e.g. "Practice Day" badge); (b) keep Type internal to logic only, no visible UI change yet | (b) for the minimum v1 — avoids a design task inside a data-migration sprint, consistent with `PROJECT_CONTEXT.md`'s lean-scope direction | **Refined, not (a) or (b) as originally framed:** Instructional Item Type will be shown subtly in Units and Planning — but **not** initially in Forecast or print. This is a v1-scope decision about *where*, not merely *whether*. **Not implemented in Sprint 1** — Sprint 1's own constraints explicitly excluded exposing Type in the UI; this decision governs a later sprint. | Low — purely cosmetic, easy to add later without migration | **Yes** |
| **D-4** | Should the initial importer write directly to the production spreadsheet, or produce a staged intermediate artifact first? | (a) Direct write (via a new guarded Apps Script function); (b) staged artifact reviewed before any write | (b), per §7's reasoning | **(b) approved as recommended.** Curriculum import will use a staged intermediate artifact, followed by preview, guarded execution, and verification. | Blocks step 7 specifically | **Yes** — steps 1–6 don't depend on this |
| **D-5** | Which `PlannedDays` initialization strategy (§6: manual / even-split-default / deferred)? | (a) Manual entry; (b) system-estimated default; (c) deferred entirely | (c) deferred, for the initial IM1 import | **(c) approved as recommended, with an explicit condition:** `PlannedDays` remains unknown by default; any initialization requires explicit teacher approval — no silent system default is ever applied. | Blocks Forecast trustworthiness (step 11), not the import itself | **Yes** — import can proceed with blank `PlannedDays` regardless of which option is eventually chosen |

None of these five blocked starting Step 1 of the implementation sequence, which is why Sprint 1 (schema contract and read compatibility — see "Sprint Progress," above) was able to begin immediately after approval. D-1 and D-3 don't gate coding until their respective consumer-update steps (Sprint 2+). D-4 and D-5 only gate the importer step specifically (7 onward). **Every decision listed here was deliberately excluded from being resolved by a backward-compatible engineering default — each one is a real teacher/product-facing tradeoff**, not a technical question this spec could safely answer on its own.

---

## 15. Definition of Done

**Information-model implementation** — done when: `Type` exists as a column, defaults correctly, and every current consumer (`sortLessons`, Forecast, Planning, Units, Print) either uses it or safely ignores it; flexible-placement items can exist without a fabricated `SortOrder`; the known/unknown numeric helper is in place everywhere `Number(x || 0)` used to be. Verified by Steps 1–6 (§10) and the Application-level checks in §11.

**Importer implementation** — done when: the preview/execute pair exists, follows the `ProductionDataCleanup.js` precedent (backup, lock, confirmation string, plan-object shared between preview and execute), and passes a full dry run against the IM1 extraction with zero unexplained discrepancies. Verified by Step 7 and the Static/Data validation levels in §11.

**Amplify IM1 data import** — done when: all 7 units exist in the production `Units`/`Lessons` sheets, matching the extraction exactly per the Data validation level in §11, with zero teacher-authored data altered and a verified restore path exercised at least once beforehand. Verified by Steps 8–10.

**Forecast readiness** — done when: every IM1 unit has a teacher-confirmed day budget (or an explicit, reviewed decision to leave a specific unit unforecast for now), and Forecast's incomplete-data state — not a silent zero — is the only thing shown for anything still unconfirmed. Separate from, and later than, the import itself per D-5's recommendation. Verified by Step 11.

**Classroom readiness** — done when: a full planning cycle (Forecast → Units → Planning → Lesson Session → Print → Teach) has been exercised against real IM1 content end to end, per `CLASSROOM_READINESS.md` Section E, with no point where the software "slows down thinking or creates uncertainty" specifically because of this import. Verified by Step 12 and is the last milestone in this document, not the same milestone as any of the four above.

---

## Non-Decisions (carried forward from the governing ADR, restated for this spec's scope)

This specification does not select: the exact `PlannedDays` allocation algorithm (§6, §14 D-5); whether `Type` labels ever appear in the teacher UI beyond v1 (§14 D-3); whether the `Lessons` sheet is ever renamed; the full Teaching Episode architecture; or the exact production import date. All remain open, by design.
